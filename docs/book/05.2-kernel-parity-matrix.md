# Kernel parity -- what would catch a wrong number

The useful question about a kernel is not *do CUDA and wgpu agree*. It is
**what turns red if this kernel starts returning subtly wrong numbers.** This
page answers that question, names the suite for each answer, and names the
kernels that have no answer.

`docs/book/05.1-wgpu-status.md` §2 is the narrative companion;
`docs/book/05-backends.md` covers backend selection and the feature graph.

## Agreement between two implementations is not an oracle

A cross-backend comparison passes whenever both sides carry the same bug. That
is not a theoretical hazard in this tree, it is the recorded failure mode:

- The wgpu w4a16 family and its own host oracle carried the same group-16
  scale-indexing bug. Every agreement test between them was green while the
  kernel was wrong at 6 of 12 group sizes. The gate that could have caught it
  is `gemv_w4a16_cpu_ref.rs` -- CUDA against a scalar host reference derived
  from the documented dequant rule, which cannot share a bug with either GPU
  path. `wgpu_gemv_w4a16_group_sizes.rs` now holds the divisibility contract.
- One ue4m3 subnormal omission existed independently in `gemv_nvfp4_v2.wgsl`,
  in two CUDA sources, and in a Rust CPU oracle. Each copy survived because the
  check it faced compared two copies of the same error, or drew inputs that
  never reached the subnormal band.

So a *census* beats a comparison: enumerate every site that implements a rule
and assert each one obeys it, from source, with no second implementation in the
loop. `wgsl_ue4m3_subnormal_census.rs` and `cuda_ue4m3_subnormal_codec.rs` are
the worked example -- they list every decode site, assert the subnormal branch
sits within a fixed distance of the bitcast, and (in
`every_shader_assembled_in_rust_is_listed_so_none_can_hide_from_the_decode_census`)
refuse to let a shader assembled in Rust hide from the enumeration. Prefer that
shape when adding a gate.

This is why the 2026-08-10 cull deleted 25 CUDA-vs-WGSL suites, and the rule it left is
the one to apply to a new one: **a cross-backend agreement test earns its place
only where the shipping backend has no independent host oracle for that entry.**

## What compares CUDA to WGSL today

Three suites, and the count is executable rather than asserted --
`feature_census.rs::EXPECT_CUDA_AND_WGPU`, checked at run time against the
`#![cfg]` header of every file in the corpus.

| suite | what it is the last gate for |
|---|---|
| `parity_gdn.rs` | `gdn_gating`, `gdn_recurrent` -- exact equality |
| `parity_gemv_bf16_i8.rs` | `rowquant_i8`, `gemv_i8_normed`, `gemv_i8_normed_mk`; the only numeric gate on the int8 GEMV epilogue, whose dropped per-row scale went uncaught by everything else |
| `parity_kv_fp8_paged.rs` | `kv_fp8_paged` -- bit-exact except the subnormal-**input** amax band, pinned by `NORMAL_SCALE_MIN_EXP` with `quantize_kv_fp8_paged_subnormal_input_band_is_the_only_gap` as the negative control |

All three were culled and then restored on 2026-08-10, because the cull's own
planted-bug pass proved a suite that cannot see them is not smaller, it is
blind. That is the standard for deleting any gate here: **plant a bug in the
code it covers and confirm something else still turns red.** A file count is
not the measurement.

**Do not reach for a `parity_*` glob to find them.** The glob resolves to four
targets and overcounts by one: `parity_verify_fused.rs` is `#![cfg(feature =
"cuda")]` and asserts the fused CUDA kernels against the *unfused CUDA* chain.
It has no WGSL side at all. `gdn_chunked_parity.rs` and
`moe_grouped_fp4_gemv_parity.rs` are likewise CUDA-vs-CUDA; "parity" in a
filename means nothing. `feature_census.rs` says so in the message of
`every_parity_and_scaling_suite_is_ABSENT_from_this_binary_SKIPPED_needs_cuda_and_wgpu`.

## What gates every other kernel

An independent host oracle on the shipping backend, under
`nv-kernels/tests/wgpu_*.rs` (`#![cfg(feature = "wgpu")]`, WGSL against a CPU
reference) or, for CUDA-only kernels, the `*_cpu_ref` suites. This is the
majority and it is the stronger gate, not a downgrade: it can fail on a bug
both backends share.

Two host-oracle shapes are weaker than they look and are worth recognising:

- **A shared oracle makes parity transitive, never direct.** The
  `quantize_nvfp4_bf16` paths assert byte-exactness against the same
  `nv_quant` oracle on both backends, so the two suites can drift to different
  oracle versions unnoticed.
- **A bound derived from the value it bounds survives every mutation.** State
  whether a tolerance was measured or picked. The loosest live one is
  `wgpu_lora.rs`'s `assert_close(rtol 2e-2, atol 2e-2)` on the bf16 output
  arm -- picked, three decades looser than anything else, on a kernel that
  fires inside `Linear::forward_inner` on every forward of every attached
  projection. Its shrink buffer is bit-exact against CUDA, so the tightening is
  probably available.

## Kernels with no live gate on the value they compute

Each of these is a **VOID INSTRUCTION**: a missing artifact, reported as a
finding rather than read as a pass. Nothing here asserts what the kernel
computes; a divergence has no detector.

| surface | what does exist | what is missing |
|---|---|---|
| `tree_verify_attn`, `tree_verify_fp8` on wgpu | `wgpu_msl_memory_census.rs` compiles both modules and pins their spill counts | no suite anywhere asserts a *value* these produce on wgpu. Spec-decode verify hot path; the worst coverage-to-criticality ratio in the crate. CUDA side is gated by `tree_verify_attn.rs` and `advkern_window_boundary.rs` |
| `assistant_drafter` (wgpu-native, a whole draft model inlined as shaders) | the same spill census; `rust/tests/wgpu_spec_assistant_unit.rs` exercises the *host* accept loop against a hash model | nothing checks any shader output |
| `flash_decode.cu::flash_splitk_fused_mk_kernel` / `..._fp8_mk_kernel` | the wgpu `_mk` entries are gated by `wgpu_flash_stage1_mk_unroll.rs` and `wgpu_flash_stage2_unroll.rs` | the CUDA side is ungated while still model-called from `nv-models/src/gemma4.rs`. The fix is a CUDA-vs-CUDA gate, not a port -- a lane sent at "port the wgpu `_mk`" would find it already there |
| `gemm_nvfp4` against CUTLASS | `wgpu_gemm_nvfp4.rs` gates both wgpu paths against a blocked CPU oracle | the CUDA/CUTLASS comparison and its characterized ULP bound went with the culled `parity_gemm_nvfp4` suite; nothing re-establishes the tile-order residual |
| rate and bandwidth, anywhere in nv-kernels | -- | there is no perf gate at all. The 19 rate suites were deleted because their only assertion was `rc == 0`, and because two of them went red when a sibling lane's GPU job moved a null control by a wild margin. A rate assertion on this box is a coin flip; a null control makes it an honest coin flip, not a kernel signal |
| driver-specific float behaviour | -- | the pins are gone with their parity files: FTZ, nvcc FMA contraction shape, the exhaustive `rsqrt` probe, the rope tie corpus. The *reasoning* survives without them -- see §Subnormals |

## Reading a green run

A suite here can pass while executing nothing, in four distinct ways.

1. **`#![cfg(all(feature = "cuda", feature = "wgpu"))]` compiles to an empty
   binary** under one feature: `0 passed` in `0.00s`, green. Nothing is left to
   panic. `feature_census.rs` exists because a sentence cannot notice a cull
   and a count can.
2. **The `parity_*` suites are opt-in.** Each takes
   `let Some(..) = backends(..) else { return; }` and prints `SKIP no CUDA
   device 0` / `SKIP no wgpu adapter`. `nvk.sh` exports
   `NV_KERNELS_PARITY_REQUIRE=1`, so nvk-driven runs are safe; a bare
   `cargo test --features cuda,wgpu` without a Vulkan loader is a false green.
3. **The `wgpu_*` convention is inverted, and is the shape to copy.** Those
   suites panic *unless* `NV_KERNELS_WGPU_ALLOW_SKIP=1` /
   `NV_KERNELS_CUDA_ALLOW_SKIP=1` is set -- a skip must be opted into, and a
   gate that cannot run says so instead of reporting success.
4. **`NV_KERNELS_WGPU_REQUIRE` is set by nothing and read by five nv-models
   suites** (`gemma4_e4b_wgpu.rs`, `gemma4_e4b_wgpu_verify.rs`,
   `gemma4_wgpu_quant.rs`, `lora_e4b_wgpu.rs`,
   `deepseek_ocr_decoder_wgpu.rs`), each of which skips silently without it.
   `nvk.sh` does not export it. Those five are green on nothing by default;
   converting them to the item-3 convention is the fix.

Every real wgpu run prints an adapter banner from `WgpuContext::summary()`
before doing work. **A wgpu result with no banner is a skip**, whatever the
result line says.

## Keeping this page's citations honest

`rust/scripts/kernel-parity-census.py` is the check. It re-derives the one-sided
enumeration from the tree, resolves every backticked symbol and every
`.rs`/`.cu`/`.wgsl`/`.md` name cited here, and ratchets the count of
`extern "C" nv_kernels_*` host fns named by no test. It owns the generated
appendix at the bottom of this page: every one-sided kernel with its recorded
verdict, regenerated by `--write` and enforced by `--check` from
`parity-audit.sh`. **The appendix follows this page's own rule -- it is derived,
never hand-edited, and a shader added without a verdict fails the gate rather
than silently joining an enumeration.**

Counts of `.cu`, `.wgsl` and entry points are deliberately **not** recorded here.
They moved by a factor of three in one commit when every static shader was
extracted from `nv-models` into `nv-kernels/wgsl/`, and a transcribed count is
wrong the moment it is read: take them fresh with `grep -c '__global__'` over
`cuda/*.cu` and `grep -c '@compute'` over `wgsl/*.wgsl`.

`feature_census.rs` is the source to trust for the suite corpus, because it is
derived at run time. It records file NAMES rather than counts, in
`CENSUS_CUDA_ONLY`, `CENSUS_CUDA_AND_WGPU`, `CENSUS_WGPU_ONLY` and
`CENSUS_UNCONDITIONAL_THESE_SUITES_READ_SOURCE_TEXT_NOT_A_DEVICE` (`EXPECT_*`
survive as their `.len()`), and asserts set equality per class, so going stale
costs one line: the failure names the file to add or delete, the constant to
edit, and the git command that finds the responsible commit. The unconditional
class is for suites whose oracle is source text; a suite that touches a device
belongs in one of the other three, and giving the offending file the header the
rest of its class carries is as often the fix as editing the list. A sibling
assertion fails on any `#![cfg]` inner attribute below line 1, because
classification reads the first line only and a `//!` block above the attribute
would silently file that suite as unconditional.

## One-sided kernels: gap, fusion, or missing test

A file-level census of `cuda/*.cu` against `wgsl/*.wgsl` sends readers at the
wrong targets, because most one-sided *files* are not ports owed. The
distinction that matters:

- **Capability gaps, real** -- `dflash_accept` and `moe_route` both run **on
  the host** on the wgpu path (`nv-specdecode/src/wgpu_spec.rs` calls the CPU
  `accept_prefix_argmax`; `nv-layers/src/moe_wgpu.rs::plan_routing` takes
  `topk_ids` as a host slice). These are per-step readbacks CUDA does not pay,
  and they are the two with a measurable cost. `dflash_gate` is a one-entry
  elementwise op and the cheapest thing on this list. `capture_copy` exists to
  feed CUDA graph capture, which wgpu has no analog for, so porting the ops is
  trivial and pointless.
- **`gdn_decode`, and the more interesting half of it** -- no wgpu counterpart,
  *and* `gdn_gating.wgsl` / `gdn_recurrent.wgsl` are ported and gated but no
  wgpu model wires them: `grep -rn gdn rust/crates/nv-models/src/` is empty.
- **Fusion gaps, running today unfused on wgpu** -- `gqa_verify_hd512`
  (hd=512 is reachable on wgpu; the specialised verify kernel is not),
  `laguna_step`, `moe_gemv`, `moe_grouped_fp4_gemv`,
  `rmsnorm_quantize_nvfp4_bf16`. Each costs a pass, not a capability.
- **Not a gap at all** -- `kv_ring`. Ring-buffer KV is folded inside the wgpu
  kernels under different entry names and gated by
  `splitk_bf16kv_ring_wrap_matches_the_linearized_cache` in
  `wgpu_flash_decode.rs`.
- **Undecided, and the largest unexamined surface in the tree** -- vendored
  `cuda/marlin/`, gated CUDA-side by `marlin_w4a16_cpu_ref.rs`, with no
  recorded "will not port" decision. `cuda_sm120/` is structural: CUTLASS
  tensor-core paths WGSL has no surface for, and `coop_mat` is not one.

Conversely, the wgpu-only modules (`assistant_drafter`, `attn_decode_small_m*`,
`fused_attn_chain`, `fused_norm_chain`, `gemm_nvfp4`, `gemv_nvfp4_lin/_v2`,
`moe_grouped_gemm`, `quant_gemv`, the `na*` modules and the `gemm_*_small_m`
shaders whose WGSL is emitted by `writeln!`) are **wgpu-native by design, not
CUDA gaps** -- they exist because wgpu needs shapes CUDA gets from cuBLASLt,
CUTLASS or a captured graph. The two generated families are invisible to any
`ls wgsl/` census, which is one more reason not to run one.

## Oracles are themselves gated: two decompositions or it is one opinion

A host reference that shares its decomposition with the kernel it checks can share
its bugs (the recorded w4a16 group-16 incident: green in every
cross-implementation comparison, wrong at 6 of 12 group sizes). So each oracle
family carries **two independent decompositions plus planted-bug negative
controls that must each be caught at every configuration**:

- `w4a16_oracle_fuzz` holds the documented dequant rule as a row-major
  f32-product oracle and a group-major all-f64 oracle, cross-checked over a seeded
  shape grid (60+ cases, groups 16-128, K 16-8192), with planted scale-index
  off-by-one, nibble-order reversal and missing sign offset. It also pins the
  incident's mechanism as a rule: **with one group per row a scale-index
  off-by-one wraps to itself**, so single-group shapes can never gate scale
  indexing and multi-group shapes are mandatory in every w4a16 suite.
- `nvfp4_oracle_fuzz` (nv-quant) cross-checks the shipping log2-route encoder
  byte-for-byte against an independent f32-bit-manipulation route over 450+ fuzzed
  blocks spanning the ue4m3 subnormal region, the 448 clamp edge, exact E2M1
  midpoints and ±0, with planted scale-mantissa truncation, inverse-from-the-
  un-roundtripped-scale, tie direction and pack order. **The un-roundtripped
  inverse is the class to fear**: a quantizer that scales by the ideal scale
  instead of the encoded-then-decoded scale is subtly wrong everywhere and exactly
  right on round-number scales.
- `gdn_host_oracle_fuzz` holds the gated-delta rule (decay, read, beta-scaled
  delta write at k, out = state-transpose q) in a kernel-order f32 reference and
  an independent rank-1-algebra f64 reference at a pinned drift tolerance, with
  planted decay-after-write, read-before-decay, missing beta and k/q confusion.
  `parity_gdn` alone is a CUDA-vs-WGSL differential with shared lineage -- these
  host references are the independent third opinion, and one test pins that **T=1
  hides every decay-ordering bug**, so multi-token sequences are mandatory in
  every GDN suite. Wiring the f64 reference as a third arm inside `parity_gdn`'s
  GPU tests is the follow-up that turns oracle self-consistency into a kernel gate.

## Generated kernel candidates are gated before they are timed

A candidate kernel is a new WGSL file under `nv-kernels/wgsl/candidates/` carrying
its own entry name (`gemv_w4a16_m1_gen0_w8`, `..._gen1_w8`, `..._gen2_w8`). **The
shipping shader is never edited by a candidate round**, so a round that loses
costs nothing and a round that wins is a routing decision made later, on its own
evidence.

`kernel_forge_gemv_w4a16` is the gate adapter. It reads candidates off disk -- no
crate rebuild per candidate -- compiles each through naga on the host, requires
the proto binding contract (packed / scale / x / y / params at bindings 0-4) and a
256-invocation entry, then dispatches at the shipping launcher's own geometry via
`gemv_w4a16_m1_proto::variant_for` and `max_v_for`. **Acceptance is not a fresh
`allclose`**: `tests/support/w4a16_host_oracle.rs` holds the two independent
decompositions and is `include!`d by both `w4a16_oracle_fuzz` and the forge, so
the candidate is graded against the same row-major f32-product and group-major f64
oracles, cross-checked at the same `3e-5` drift bound before grading, and accepted
only inside the `1e-2` relative bound `gemv_w4a16_cpu_ref` and
`gemv_w4a16_m1_proto` already pin. One oracle, not a copy of one.

Three rules make the pipeline worth trusting. **Gate first, time second**:
latency lives in a separate `#[ignore]` test the runner invokes only after the
correctness gate is green, because a faster kernel that is wrong is not a result
and a number taken before the gate is not evidence. **The candidate set always
carries a planted-bug member**: `gemv_w4a16_m1_planted_bug_scale_off_by_one` reads
the next group's scale -- the recorded group-16 incident class -- and the gate
asserts it is REJECTED at every shape, so if it ever passes, every green candidate
in that run is void (gate shapes are multi-group for the same reason). **Failures
are recycled, not discarded**: compile, dispatch and oracle failures append
`stem<TAB>stage<TAB>detail` to `$NV_KERNEL_FORGE_FAILURE_LOG` and the next attempt
prints them before it generates anything.

`nv-kernels/tools/kernel-forge.sh [gate|bench|all] [-c stem,stem]` is the runner:
its own `NVK_LANE`, logs outside the source tree, and a bounded wait on
`nvidia-smi memory.used` before every GPU run, so a timed arm never shares the
box. The gate arm needs a GPU only for the dispatch; the oracle, the planted-bug
rejection proof and the candidate compile all run on the host.

Cycle one on `gemv_w4a16_m1_proto` (wgpu/Vulkan, one NVIDIA Blackwell adapter,
8 warm-up plus 200 timed dispatches per arm in one pass, GPU idle at 2 MiB, two
runs) settled the shape of the win: all three candidates pass the oracle at
`(67,320)`, `(96,2560)`, `(33,10240)` with max rel `3.2e-3` to `3.8e-3`, the
planted-bug candidate is rejected at all three, and against the shipping shader at
N=20480 K=2560 the barrier-count variant `gen0` and the dual-accumulator variant
`gen1` land inside run-to-run spread while the `vec4<u32>`-load variant `gen2`
roughly halves the kernel time at both shapes. **Wide loads are the lever on this
kernel; barrier count and ILP are not.** Nothing routes to `gen2` on that evidence
alone -- the shipping wgpu path changes only through the performance-default rule.

## Subnormals are the one class no adapter can be promised bit-exact

CUDA builds with nvcc's `--ftz=false` default: denormals preserved. Vulkan/WGSL
exposes no denormal mode to a shader -- the WGSL floating-point rules leave
subnormal handling implementation-defined, and
`VkPhysicalDeviceFloatControlsProperties` only *reports* flush-to-zero
behaviour, it does not let a shader select it. Any cross-backend gate must
therefore scope bit-exactness to chains with no subnormal intermediate and
bound the tainted lanes separately. `parity_kv_fp8_paged.rs` is the worked
example.

Only integer, copy and LUT surfaces are spec-deterministic and should transfer
across adapters: gather_rows, moe_permute, the token_map / incr_pos halves of
graph_decode, KV byte-moves, and the e2m1 / ue4m3 table decodes. Everything
else on this page was executed on one NVIDIA Vulkan adapter. Metal coverage is
separate and functional-only (`docs/book/05.6-macos-port-status.md`), with
`wgpu_msl_memory_census.rs` as the one MSL audit that exists -- and its own
history is this page's failure mode in miniature: a predecessor asserted
`!msl.contains("thread_local")`, a string naga never emits, so it passed while
claiming the opposite of what the census records for the same entries.

## What a wgpu decode step still lacks

**Kernel coverage is not the blocker.** Every primitive of a dense decode step
exists on wgpu -- embed, rope, the norm families, attention in four shapes, KV
read/write, the GEMV/GEMM families, and logits-to-token. What is missing is
structural, and the ordering matters: **buffer lifetime comes first.**
`wgpu_backend/buffer.rs` defines `GpuTensor<T>` and `dispatch.rs` defines
`run_resident`, but `run_resident` has exactly one occurrence in
`nv-kernels/src/` -- its definition -- and no kernel module uses `GpuTensor`
either; it is consumed *above* nv-kernels, in `nv-layers` and heavily in
`nv-models` and `nv-specdecode`, while the kernel entry fns still take host
slices. Threading `GpuTensor` through them is the single highest-leverage change,
and it is now a migration with callers waiting, not a design. A command-buffer
reuse layer -- wgpu's equivalent of what `CudaGraphRunner` amortizes -- only
becomes possible after it, and **the two host-resident steps are made worse
rather than better by it**: MoE routing and spec-decode accept run on the host, so
a device-resident tensor that has to come back for routing buys less than it
looks. `tree_verify_*` remains without a wgpu value gate, so a wgpu spec-decode
verify step would run two modules whose output nothing checks.

## Appendix: every one-sided kernel, with its verdict

<!-- kernel-parity-census: BEGIN GENERATED -->

### Inventory, counted from disk

| surface | count |
|---|---|
| top-level `cuda/*.cu` | 43 (138 `__global__`) |
| vendored `cuda/marlin/**/*.cu` | 3 (152 `__global__`) |
| `cuda_sm120/*.cu` | 3 (1 `__global__`; CUTLASS device code is template-instantiated, so this undercounts what runs) |
| `wgsl/*.wgsl` | 118 (370 `@compute`) |
| modules under `src/wgpu_backend/kernels/` | 45 |
| kernel modules one level up in `src/wgpu_backend/` | 4 (`dequant`, `na`, `na_attn`, `na_bf16`) |
| nv-kernels test files | 111 |

Entry-point totals are `__global__` and `@compute` counts. A kernel written in Rust
with `writeln!` has no `.wgsl` file and is counted as a module, not as a shader.

### CUDA-only: a `.cu` with no `.wgsl` and no wgpu module

| kernel | EP | entry points | tests naming it | called from | classification |
|---|---|---|---|---|---|
| `capture_copy.cu` | 2 | `copy_cols_bf16_kernel`, `mul_sigmoid_rowgate_f32_kernel` | none | `nv-layers`, `nv-models` | **capability gap** -- the two ops are trivial; what does not transfer is their reason for existing -- they feed CUDA graph capture, and wgpu has no graph/replay analog |
| `dflash_accept.cu` | 3 | `dflash_row_argmax_stage1`, `dflash_row_argmax_stage2`, `dflash_accept_chain_kernel` | `nv-models/tests/laguna_dflash_verify.rs` | `nv-models` | **capability gap** -- wgpu spec-decode runs the accept chain on the host -- `nv-specdecode/src/wgpu_spec.rs` calls the CPU `accept_prefix_argmax` -- so it pays a logits readback per step |
| `dflash_gate.cu` | 1 | `softplus_gate_bf16_kernel` | none | `nv-models` | **capability gap** -- cheapest on the list: one elementwise softplus gate, and WGSL already spells the function as `gdn_softplus_safe` in `gdn_gating.wgsl` |
| `gdn_decode.cu` | 4 | `gdn_conv_decode_silu_bf16_kernel`, `gdn_conv_decode_chunk_silu_bf16_kernel`, `gdn_decode_step_bf16_kernel`, `gdn_decode_chunk_bf16_kernel` | `nv-kernels/tests/gdn_chunked_parity.rs`, `nv-kernels/tests/smem_optin_never_lowers.rs` | `nv-layers` | **capability gap** -- and the interesting half is the other direction: `gdn_gating.wgsl` / `gdn_recurrent.wgsl` are gated A by `parity_gdn.rs` and no wgpu model wires them -- `nv-layers/src/linear_attn.rs` dispatches CUDA or falls back to candle on the host |
| `gqa_verify_hd512.cu` | 3 | `gqa512_stage1_kernel`, `gqa512_stage1_fp8_kernel`, `gqa512_stage2_kernel` | `nv-kernels/tests/gqa_hd512_verify.rs` | `nv-models` | **fusion gap** -- head_dim 512 is reachable on wgpu (`MAX_HEAD_DIM` in `kernels/flash_decode.rs`, `FD_MAX_HD` in `flash_decode.wgsl`); only the multi-token verify specialisation is CUDA-only |
| `kv_ring.cu` | 3 | `kv_ring_append_bf16_kernel`, `kv_shift_bf16_kernel`, `attention_bf16_decode_ring_kernel` | none | `nv-models` | **not a gap** -- ring KV is folded inside the wgpu kernels rather than standing alone, and `splitk_bf16kv_ring_wrap_matches_the_linearized_cache` in `wgpu_flash_decode.rs` gates it |
| `laguna_step.cu` | 7 | `laguna_rope_scale_bf16_kernel`, `laguna_rstd256_bf16_kernel`, `laguna_qk_normrope_bf16_kernel`, `laguna_flash_decode_gqa_kernel`, `laguna_seqlens_prep_kernel`, `softplus_gate_exact_bf16_kernel`, `prof_timestamp_kernel` | `nv-kernels/tests/feature_census.rs`, `nv-kernels/tests/laguna_flash_decode_gqa.rs` | `nv-layers`, `nv-models` | **fusion gap** -- `nv-models/src/laguna_wgpu/` is a real wgpu Laguna decoder composed of generic kernels |
| `moe_gemv.cu` | 4 | `moe_gemv_swiglu_m1_kernel`, `moe_gemv_swiglu_mb_kernel`, `moe_gemv_down_tail_m1_kernel`, `moe_gemv_down_tail_mb_kernel` | `nv-kernels/tests/guard_refusal_gemv.rs`, `nv-kernels/tests/moe_gemv.rs` | `nv-models` | **fusion gap** -- `moe_grouped_gemm.wgsl` covers grouped MoE matmul; the fused swiglu epilogue is what is missing |
| `moe_grouped_fp4_gemv.cu` | 1 | `moe_grouped_fp4_gemv_m1_bf16_kernel` | `nv-kernels/tests/guard_refusal_gemv.rs`, `nv-kernels/tests/moe_grouped_fp4_gemv_bench.rs`, `nv-kernels/tests/moe_grouped_fp4_gemv_parity.rs` | `nv-layers` | **fusion gap** -- as `moe_gemv`, plus the `gemv_nvfp4*` family for the fp4 half |
| `moe_route.cu` | 2 | `moe_route_topk_kernel`, `gather_f32_by_ids_kernel` | none | `nv-layers`, `nv-models` | **capability gap** -- routing runs on the host: `plan_routing` in `nv-layers/src/moe_wgpu.rs` takes `topk_ids` as a host slice, and no WGSL entry computes a topk -- `moe_permute.wgsl` consumes `mp_topk_ids`, it does not produce them |
| `rmsnorm_quantize_nvfp4_bf16.cu` | 1 | `rmsnorm_quantize_nvfp4_bf16_kernel` | `nv-kernels/tests/cuda_ue4m3_subnormal_codec.rs` | `nv-layers` | **fusion gap** -- `rmsnorm.wgsl` and `quantize_nvfp4_bf16.wgsl` both exist and are gated; composing them costs one pass |
| `marlin/` | 152 | `entry.cu`, `sm80_kernel_bfloat16_u4b8_bfloat16.cu`, `sm80_kernel_float16_u4b8_float16.cu` | `nv-kernels/tests/feature_census.rs`, `nv-kernels/tests/gemv_w4a16_m1_proto.rs`, `nv-kernels/tests/laguna_fp8_contract.rs`, +3 more | -- | **capability gap** -- undecided: still no recorded will-not-port decision, and the largest unexamined surface in the tree |
| `cuda_sm120/` | 1 | `cutlass_fp4_gemm.cu`, `gemv_blockscaled_probe.cu`, `moe_grouped_fp4_gemm.cu` | `nv-kernels/tests/cutlass_fp4_gemm_sm120.rs`, `nv-kernels/tests/feature_census.rs`, `nv-kernels/tests/moe_grouped_fp4_multi_expert.rs`, +1 more | -- | **capability gap** -- structural: CUTLASS/sm_120 tensor-core paths, mostly template-instantiated rather than `__global__`-declared, and WGSL has no equivalent surface -- `coop_mat` is not one |

### wgpu-only: a `.wgsl` or a wgpu module with no `.cu`

| module / wgsl | wgsl EP | tests naming it | classification |
|---|---|---|---|
| `assistant_drafter` | 11 | `nv-kernels/tests/wgpu_msl_memory_census.rs` | **wgpu-native by design** -- a whole draft model inlined as shaders; CUDA drafts through the normal model path |
| `attn_decode_small_m` | 6 | `nv-kernels/tests/wgpu_attn_small_m.rs`, `nv-kernels/tests/wgpu_attn_small_m_v2.rs`, `nv-kernels/tests/wgpu_msl_memory_census.rs` | **wgpu-native by design** -- small-m specialisation CUDA does not need |
| `attn_decode_small_m_fp8` | 4 | `nv-kernels/tests/wgpu_attn_small_m.rs`, `nv-kernels/tests/wgpu_attn_small_m_fp8.rs`, `nv-kernels/tests/wgpu_msl_memory_census.rs` | **wgpu-native by design** -- small-m specialisation CUDA does not need |
| `attn_decode_small_m_v2` | generated in Rust | `nv-kernels/tests/wgpu_attn_small_m_v2.rs` | **wgpu-native by design** -- WGSL emitted in Rust, invisible to any `ls wgsl/` census |
| `dequant` | 0 | `nv-kernels/tests/wgpu_correct_gemv_nvfp4_variants.rs`, `nv-kernels/tests/wgpu_dequant.rs`, `nv-kernels/tests/wgpu_fp8_group_gemv.rs`, +9 more | **wgpu-native by design** -- a WGSL fn library with no entry points; CUDA dequant lives in `include/` headers |
| `e4b_axpby` | 1 | none | **not a gap** -- `residual_scale.cu`'s `residual_add_scale_bf16_kernel` is the same a*x+b*y |
| `e4b_flash2_pk_mk` | 2 | none | **not a gap** -- `flash_decode.cu`'s `flash_splitk_fused_fp8_kernel` and `flash_splitk_fused_fp8_mk_kernel` carry the fp8 splitk attention; `gemma4_e4b.rs` drives the fused flash decode entry points on CUDA |
| `e4b_fncu_prelude` | 0 | none | **fragment** -- no entry points; a prelude `gemma4_e4b_wgpu.rs` prepends to shaders needing the fncu helpers |
| `e4b_gatemul` | 2 | none | **not a gap** -- `gelu_tanh_mul.cu`'s `gelu_tanh_mul_bf16_kernel` is the same gelu-gate multiply |
| `e4b_gather` | 2 | none | **not a gap** -- `gather_rows_bf16.cu`'s `gather_rows_bf16_kernel` gathers the same rows |
| `e4b_gemv_pk_mk` | 2 | `nv-models/tests/graph_entry_census_oracle.rs`, `nv-models/tests/graph_g4w_mk_bf16_oracle.rs` | **not a gap** -- `gemv_bf16.cu`'s `gemm_bf16_mk_kernel` is the same multi-row gemm |
| `e4b_gemv_w4_pk` | 8 | none | **not a gap** -- `gemv_w4a16.cu` and the marlin w4a16 kernels carry the same 4-bit gemv/gemm family; block/v4 and pk/pk3 are wgpu packing variants |
| `e4b_lmhead_i8` | 1 | none | **not a gap** -- `gemv_bf16.cu`'s `gemv_i8_normed_kernel` over vocab rows is the same int8 lm head |
| `e4b_lora_cvt` | 2 | none | **not a gap** -- `lora_fused.cu` and `lora_grouped.cu` consume bf16 factors directly; widen/repack exist because wgpu stores lora factors as packed u32 words |
| `e4b_smk_tripwire` | 2 | none | **backend probe** -- smk_trip_clean/smk_trip_poisoned detect an adapter whose subgroup-matrix path miscompiles, at pipeline-build time in `gemma4_e4b_wgpu.rs`; they compute no model math and CUDA has nothing equivalent to probe |
| `e4b_verify_cap` | 1 | none | **not a gap** -- `residual_scale.cu`'s `tanh_softcap_bf16_to_f32_kernel` is the same logit softcap, driven from `gemma4.rs` |
| `flash_decode_fold_epilogue` | 0 | none | **not a gap** -- CUDA has no __global__ fold at all: flash_decode.cu folds its splits inside the splitk kernel with fan_in and atomics (47 fan_in, 6 atomicAdd, 12 __threadfence). wgpu has no equivalent primitive, so the same reduction becomes its own pass. Same math, decomposed differently because the backend forces it |
| `flash_decode_fold_head` | 0 | none | **not a gap** -- CUDA has no __global__ fold at all: flash_decode.cu folds its splits inside the splitk kernel with fan_in and atomics (47 fan_in, 6 atomicAdd, 12 __threadfence). wgpu has no equivalent primitive, so the same reduction becomes its own pass. Same math, decomposed differently because the backend forces it |
| `flash_decode_fold_reduce_sg` | 0 | none | **not a gap** -- CUDA has no __global__ fold at all: flash_decode.cu folds its splits inside the splitk kernel with fan_in and atomics (47 fan_in, 6 atomicAdd, 12 __threadfence). wgpu has no equivalent primitive, so the same reduction becomes its own pass. Same math, decomposed differently because the backend forces it |
| `flash_decode_fold_reduce_wb` | 0 | none | **not a gap** -- CUDA has no __global__ fold at all: flash_decode.cu folds its splits inside the splitk kernel with fan_in and atomics (47 fan_in, 6 atomicAdd, 12 __threadfence). wgpu has no equivalent primitive, so the same reduction becomes its own pass. Same math, decomposed differently because the backend forces it |
| `flash_decode_fold_rounds` | 0 | none | **not a gap** -- CUDA has no __global__ fold at all: flash_decode.cu folds its splits inside the splitk kernel with fan_in and atomics (47 fan_in, 6 atomicAdd, 12 __threadfence). wgpu has no equivalent primitive, so the same reduction becomes its own pass. Same math, decomposed differently because the backend forces it |
| `fused_attn_chain` | 3 | `nv-kernels/tests/wgpu_fused_attn_chain.rs`, `nv-kernels/tests/wgpu_msl_memory_census.rs` | **wgpu-native by design** -- the wgpu answer to CUDA graph capture |
| `fused_norm_chain` | 3 | `nv-kernels/tests/wgpu_flash_zeroinit_ab.rs`, `nv-kernels/tests/wgpu_fused_norm_chain.rs`, `nv-kernels/tests/wgpu_msl_memory_census.rs`, +1 more | **wgpu-native by design** -- the wgpu answer to CUDA graph capture |
| `g4a_unpack_hidden` | 1 | none | **not a gap** -- `graph_decode.cu`'s `cast_bf16_f32_kernel` is the same bf16-to-f32 widen; the packed-u32 staging exists only because wgpu buffers are word-addressed, and the CUDA drafter in nv-specdecode reads backbone hidden states directly |
| `g4m_attn` | 3 | `nv-models/tests/wgpu_nozi_graph_census.rs`, `nv-models/tests/wgpu_nozi_graph_poison_parity.rs` | **not a gap** -- norm+rope reach `rmsnorm.cu` and `rope.cu` through nv-layers; the kv write and decode attention are candle ops in `gemma4_moe.rs`, which composes the CUDA forward from nv-layers and candle rather than in-tree attention kernels |
| `g4m_gemv_bf16` | 1 | `nv-models/tests/gemma4_moe_gemv_wide.rs`, `nv-models/tests/gemma4_moe_gemv_wide_ab.rs`, `nv-models/tests/gemma4_moe_int8_dense_shapes.rs`, +1 more | **not a gap** -- nv-layers `linear.rs` routes this family's projections to `gemv_bf16.cu` on CUDA |
| `g4m_gemv_bf16_legacy` | 1 | none | **not a gap** -- the same bf16 gemv; legacy is the no-subgroup reduction spelling |
| `g4m_gemv_i8` | 1 | `nv-models/tests/gemma4_moe_int8_dense_shapes.rs`, `nv-models/tests/gemma4_moe_nozi_price.rs` | **not a gap** -- `gemv_bf16.cu`'s `gemv_i8_normed_kernel` is the same int8 gemv |
| `g4m_gemv_i8_v4` | 1 | `nv-models/tests/gemma4_moe_nozi_price.rs` | **not a gap** -- the same int8 gemv, vec4 spelling |
| `g4m_gemv_w4e` | 2 | `nv-models/tests/gemma4_moe_w4_gemv_ab.rs` | **not a gap** -- the 4-bit expert gemv: CUDA holds Gemma4Moe experts as nvfp4 and reaches `gemv_nvfp4.cu` through `linear.rs`, wgpu packs them w4a16 -- same function, different 4-bit container |
| `g4m_moe` | 9 | `nv-models/tests/gemma4_moe_router_topk_ab.rs` | **wgpu-native by design** -- the CUDA Gemma4Moe forward composes routing, gelu, combine and argmax from candle ops -- `gemma4_moe.rs` imports no nv-kernels -- so these shaders exist because wgpu-candle has no usable equivalents, not because CUDA is missing them |
| `g4m_prefill_head` | 10 | `nv-kernels/tests/wgsl_ue4m3_subnormal_census.rs` | **wgpu-native by design** -- CUDA prefill for this family is candle matmuls (cuBLAS); the pm_* fused prefill pipeline exists because wgpu has no cuBLAS to lean on |
| `g4m_prop_norm` | 2 | none | **not a gap** -- `rmsnorm.cu` and `rmsnorm_residual.cu` via nv-layers RmsNorm |
| `g4shared_flash2_pk` | 1 | `nv-models/tests/e4b_dispatch_floor.rs`, `nv-models/tests/wgpu_shader_drift_guard.rs` | **not a gap** -- `flash_decode.cu`'s `flash_splitk_stage2_kernel` is the same splitk stage-2 reduction |
| `g4shared_gemv_pk` | 2 | `nv-models/tests/wgpu_shader_drift_guard.rs` | **not a gap** -- `gemv_bf16.cu`'s `gemv_bf16_kernel`; vec8 is a wgpu vectorisation width |
| `g4shared_rope_f32` | 1 | `nv-models/tests/wgpu_shader_drift_guard.rs` | **not a gap** -- `rope.cu`'s `rope_kernel` and `rope_bf16.cu` are the same rotation |
| `g4w_fp8_pk_bindings` | 0 | none | **fragment** -- no entry points; the shared fp8 binding block `gemma4_wgpu.rs` textually composes into the g4w fp8 pk shaders |
| `g4w_gemv4_pk_sg` | 1 | `nv-kernels/tests/wgpu_gemv_nvfp4_variants.rs`, `nv-models/tests/graph_entry_census_oracle.rs` | **not a gap** -- `gemv_nvfp4.cu`'s `nvfp4_gemv_bf16_kernel` is the same nvfp4 gemv; _sg is the subgroup-reduction spelling for adapters that have subgroups |
| `g4w_gemv4_pk_tree` | 1 | `nv-kernels/tests/wgpu_gemv_nvfp4_variants.rs`, `nv-models/tests/graph_entry_census_oracle.rs` | **not a gap** -- the same nvfp4 gemv against `gemv_nvfp4.cu`; _tree is the no-subgroup reduction, an adapter split CUDA does not need because warp shuffles always exist |
| `g4w_gemv_q8_pk_sg_legacy` | 2 | `nv-models/tests/graph_entry_census_oracle.rs`, `nv-models/tests/wgpu_fp8_epilogue.rs` | **not a gap** -- `gemv_bf16.cu`'s `gemv_i8_normed_kernel` family carries the int8 gemv; legacy is the pre-template spelling kept for adapters the template path rejects |
| `g4w_gemv_q8_pk_sg_prologue` | 0 | none | **fragment** -- no entry points; the shared prologue `gemma4_wgpu.rs` prepends when instantiating the q8 sg template |
| `g4w_gemv_q8_pk_sg_template` | 2 | none | **fragment** -- its entries are `g4w_gemv_TAG_pk` placeholders -- `gemma4_wgpu.rs` substitutes TAG at pipeline build, so the census sees a kernel that never runs under this name |
| `g4w_gemv_q8_pk_tree_legacy` | 2 | `nv-models/tests/graph_entry_census_oracle.rs`, `nv-models/tests/wgpu_fp8_epilogue.rs` | **not a gap** -- the same int8 gemv as the sg legacy file against `gemv_bf16.cu`, with the tree reduction for adapters without subgroups |
| `g4w_gemv_q8_pk_tree_prologue` | 0 | none | **fragment** -- no entry points; the tree-variant prologue `gemma4_wgpu.rs` prepends when instantiating the q8 tree template |
| `g4w_gemv_q8_pk_tree_template` | 2 | none | **fragment** -- TAG-placeholder entries substituted by `gemma4_wgpu.rs` at pipeline build, same as the sg template |
| `g4w_glue` | 3 | `nv-models/tests/graph_entry_census_oracle.rs`, `nv-models/tests/graph_g4w_gather2_oracle.rs` | **not a gap** -- gather2_bf16 and gather2_bf16_mk are `gather_rows_bf16.cu`'s `gather_rows_bf16_kernel`; pack_lo16 exists because wgpu buffers are u32-word addressed where CUDA reads bf16 pointers directly |
| `g4w_head_prep` | 1 | `nv-models/tests/graph_entry_census_oracle.rs`, `nv-models/tests/graph_g4w_head_prep_oracle.rs`, `nv-models/tests/wgpu_nozi_graph_census.rs` | **not a gap** -- computes the per-head kind/index table the pk attention dispatch consumes; CUDA passes head geometry as kernel launch arguments and needs no table |
| `g4w_mk_params` | 0 | `nv-kernels/tests/wgsl_ue4m3_subnormal_census.rs`, `nv-models/tests/graph_g4w_mk_bf16_oracle.rs`, `nv-models/tests/wgpu_fp8_epilogue.rs` | **fragment** -- no entry points; the mk params struct block composed into the mk shaders by `gemma4_wgpu.rs` and `gemma4_e4b_wgpu.rs` |
| `g4w_norm_chain` | 2 | `nv-models/tests/graph_entry_census_oracle.rs`, `nv-models/tests/graph_g4w_norm_chain_oracle.rs` | **not a gap** -- `rmsnorm_residual.cu`'s `rmsnorm_residual_kernel_bf16` and `rmsnorm.cu`'s `rmsnorm_kernel` are the same fused norm chains |
| `g4w_quant_pk` | 1 | `nv-models/tests/graph_entry_census_oracle.rs`, `nv-models/tests/graph_g4w_quant_row_oracle.rs` | **not a gap** -- `gemv_bf16.cu`'s `rowquant_i8_kernel` quantises the same rows |
| `gemm_bf16_small_m` | generated in Rust | `nv-kernels/tests/wgpu_gemm_bf16_small_m.rs`, `nv-kernels/tests/wgpu_msl_memory_census.rs`, `nv-kernels/tests/wgsl_ue4m3_subnormal_census.rs` | **wgpu-native by design** -- WGSL emitted by `writeln!` in the module; CUDA reaches this through cuBLASLt |
| `gemm_coop_f16` | generated in Rust | `nv-kernels/tests/wgpu_gemm_coop_prefill.rs`, `nv-kernels/tests/wgsl_ue4m3_subnormal_census.rs` | **wgpu-native by design** -- WGSL emitted by `writeln!` in the module; CUDA reaches this through cuBLASLt |
| `gemm_nvfp4` | 2 | `nv-kernels/tests/wgpu_gemm_nvfp4.rs`, `nv-kernels/tests/wgpu_moe_grouped.rs`, `nv-kernels/tests/wgpu_msl_memory_census.rs`, +2 more | **wgpu-native by design** -- CUDA uses CUTLASS (`cuda_sm120/cutlass_fp4_gemm.cu`) |
| `gemm_w4a16_small_m` | generated in Rust | `nv-kernels/tests/wgpu_gemm_w4a16_small_m.rs`, `nv-kernels/tests/wgpu_msl_memory_census.rs`, `nv-kernels/tests/wgsl_ue4m3_subnormal_census.rs` | **wgpu-native by design** -- WGSL emitted by `writeln!` in the module; CUDA reaches this through cuBLASLt |
| `gemv_bf16_sg` | 6 | `nv-kernels/tests/wgpu_msl_memory_census.rs`, `nv-kernels/tests/wgsl_ue4m3_subnormal_census.rs` | **not a gap** -- a subgroup is what CUDA gets for free as a warp -- gemv_bf16.cu's gemv_bf16_kernel already reduces with __shfl_xor_sync, so it needs no separately named subgroup variant |
| `gemv_bf16_sg_pk` | 2 | `nv-kernels/tests/wgpu_msl_memory_census.rs` | **not a gap** -- a subgroup is what CUDA gets for free as a warp -- gemv_bf16.cu's gemv_bf16_kernel already reduces with __shfl_xor_sync, so it needs no separately named subgroup variant |
| `gemv_nvfp4_lin` | 8 | `nv-kernels/tests/wgpu_msl_memory_census.rs`, `nv-kernels/tests/wgsl_ue4m3_subnormal_census.rs` | **wgpu-native by design** -- layout/routing variant with no CUDA analog |
| `gemv_nvfp4_v2` | 9 | `nv-kernels/tests/qwen35_opt_gemv_route.rs`, `nv-kernels/tests/wgpu_gemv_nvfp4_v2_routed_pk.rs`, `nv-kernels/tests/wgpu_gemv_nvfp4_variants.rs`, +11 more | **wgpu-native by design** -- layout/routing variant with no CUDA analog |
| `gow_attn` | 4 | `nv-models/tests/graph_entry_census_oracle.rs`, `nv-models/tests/wgpu_nozi_graph_census.rs` | **no cuda model** -- gpt-oss is served only on wgpu: nv-models has gpt_oss_wgpu.rs and no CUDA sibling, and chat_engine/build.rs's ModelFamily enumerates Qwen3, Gemma4, Gemma4E4b, Gemma4Moe, Qwen3_5Moe and Laguna with no GptOss variant. Nothing is missing from the CUDA side of this kernel because there is no CUDA side of this model |
| `gow_gemv` | 1 | none | **no cuda model** -- gpt-oss is served only on wgpu: nv-models has gpt_oss_wgpu.rs and no CUDA sibling, and chat_engine/build.rs's ModelFamily enumerates Qwen3, Gemma4, Gemma4E4b, Gemma4Moe, Qwen3_5Moe and Laguna with no GptOss variant. Nothing is missing from the CUDA side of this kernel because there is no CUDA side of this model |
| `gow_moe` | 6 | `nv-models/tests/graph_entry_census_oracle.rs`, `nv-models/tests/graph_gow_router_topk_oracle.rs` | **no cuda model** -- gpt-oss is served only on wgpu: nv-models has gpt_oss_wgpu.rs and no CUDA sibling, and chat_engine/build.rs's ModelFamily enumerates Qwen3, Gemma4, Gemma4E4b, Gemma4Moe, Qwen3_5Moe and Laguna with no GptOss variant. Nothing is missing from the CUDA side of this kernel because there is no CUDA side of this model |
| `gow_mx` | 1 | `nv-models/tests/gpt_oss_wgpu_chunked_prefill.rs`, `nv-models/tests/graph_entry_census_oracle.rs` | **no cuda model** -- gpt-oss is served only on wgpu: nv-models has gpt_oss_wgpu.rs and no CUDA sibling, and chat_engine/build.rs's ModelFamily enumerates Qwen3, Gemma4, Gemma4E4b, Gemma4Moe, Qwen3_5Moe and Laguna with no GptOss variant. Nothing is missing from the CUDA side of this kernel because there is no CUDA side of this model |
| `gow_prefill` | 8 | `nv-models/tests/wgpu_nozi_graph_census.rs` | **no cuda model** -- gpt-oss is served only on wgpu: nv-models has gpt_oss_wgpu.rs and no CUDA sibling, and chat_engine/build.rs's ModelFamily enumerates Qwen3, Gemma4, Gemma4E4b, Gemma4Moe, Qwen3_5Moe and Laguna with no GptOss variant. Nothing is missing from the CUDA side of this kernel because there is no CUDA side of this model |
| `laguna_attn` | 4 | `nv-models/tests/laguna_wgpu_attn.rs` | **wgpu-native by design** -- CUDA laguna gets attention from FlashAttention-2, not from a kernel in this tree: laguna_fa2.rs is a cfg(cuda) FFI shim declaring FlashFwdParams and laguna_step_graph.rs drives it. wgpu has no FA2 to bind, so the attention is spelled here |
| `laguna_common` | 4 | none | **not a gap** -- four entry points and CUDA reaches all four elsewhere: lgw_silu_mul against silu.cu, lgw_argmax_stage1/stage2 against the argmax in graph_decode.cu and dflash_accept.cu, and lgw_gather_embed against candle's own gather -- no CUDA file in this tree spells an embedding gather because nothing needs to |
| `laguna_gemv_bf16` | 2 | none | **not a gap** -- gemv_bf16.cu carries the bf16 gemv family this splits out |
| `laguna_gemv_i8` | 2 | none | **not a gap** -- gemv_bf16.cu's gemv_i8_normed_kernel is the same int8 gemv |
| `laguna_gemv_nvfp4` | 1 | none | **not a gap** -- gemv_nvfp4.cu's nvfp4_gemv_bf16_kernel is the same nvfp4 gemv under a transposed name -- the census reads it as one-sided only because the words are ordered the other way |
| `laguna_moe` | 2 | none | **not a gap** -- the CUDA MoE path is five files -- moe_route.cu, moe_permute.cu, moe_gemv.cu, moe_grouped_fp4_gemv.cu and moe_unpermute_scatter.cu -- against one shader here |
| `laguna_quant_rows` | 1 | `nv-models/tests/laguna_wgpu_w8.rs` | **not a gap** -- gemv_nvfp4.cu's nvfp4_quantize_row_bf16_kernel quantises the same rows |
| `moe_grouped_gemm` | 7 | `nv-kernels/tests/wgpu_moe_grouped.rs`, `nv-kernels/tests/wgpu_msl_memory_census.rs`, `nv-layers/tests/moe_wgpu.rs` | **wgpu-native by design** -- CUDA uses `moe_gemv.cu` plus `cuda_sm120/moe_grouped_fp4_gemm.cu` |
| `na` | generated in Rust | `nv-kernels/tests/wgpu_na_attn.rs`, `nv-kernels/tests/wgpu_na_gemm.rs`, `nv-kernels/tests/wgpu_na_gemm_bf16.rs`, +3 more | **wgpu-native by design** -- prefill-shaped GEMM, WGSL generated in Rust, module outside `kernels/` |
| `na_attn` | generated in Rust | `nv-kernels/tests/wgpu_na_attn.rs`, `nv-models/tests/gemma4_e4b_wgpu_na_attn.rs` | **wgpu-native by design** -- prefill-shaped attention, WGSL generated in Rust, module outside `kernels/` |
| `na_bf16` | generated in Rust | `nv-kernels/tests/wgpu_na_gemm_bf16.rs` | **wgpu-native by design** -- prefill-shaped bf16 GEMM, WGSL generated in Rust, module outside `kernels/` |
| `q3d_attn` | 4 | `nv-models/tests/graph_q3d_attn_chain_oracle.rs`, `nv-models/tests/graph_q3d_attn_oracle.rs`, `nv-models/tests/wgpu_nozi_graph_census.rs`, +1 more | **not a gap** -- dense layers share the qwen3.5 CUDA path: `rmsnorm.cu`/`rope.cu` via nv-layers, `attention_fp8_decode.cu` for decode, candle sigmoid for the output gate |
| `q3d_delta` | 5 | `nv-models/tests/graph_entry_census_oracle.rs`, `nv-models/tests/graph_q3d_delta_decode_oracle.rs`, `nv-models/tests/graph_q3d_delta_front_oracle.rs`, +4 more | **not a gap** -- same chain as the moe file: `depthwise_conv1d_silu_bf16.cu`, `gdn_gating.cu`, `gdn_decode.cu`/`gdn_recurrent.cu` |
| `q3d_gemv_bf16` | 1 | `nv-models/tests/qwen3_5_moe_gemv_bf16_load.rs` | **not a gap** -- `gemv_bf16.cu` |
| `q3d_gemv_i8` | 2 | `nv-models/tests/graph_entry_census_oracle.rs`, `nv-models/tests/graph_q3d_gemv_i8_oracle.rs` | **not a gap** -- `gemv_bf16.cu`'s `gemv_i8_normed_kernel`; i8g is the grouped-scale form of the same gemv |
| `q3d_misc` | 4 | `nv-models/tests/graph_entry_census_oracle.rs`, `nv-models/tests/graph_q3d_decode_head_oracle.rs`, `nv-models/tests/graph_q3d_elementwise_oracle.rs`, +1 more | **not a gap** -- `silu.cu`'s `silu_mul_kernel`, candle gather for the embedding, and `graph_decode.cu`'s argmax stage pair |
| `q3d_prefill` | 11 | `nv-models/tests/graph_entry_census_oracle.rs`, `nv-models/tests/graph_q3d_attn_chain_oracle.rs`, `nv-models/tests/graph_q3d_delta_decode_oracle.rs`, +4 more | **not a gap** -- the _m entries are batched spellings of the decode entries above; CUDA prefill reaches the same math through the same kernels plus cuBLAS GEMMs |
| `q3m_attn` | 4 | `nv-models/tests/graph_q3d_attn_chain_oracle.rs`, `nv-models/tests/graph_q3d_attn_oracle.rs`, `nv-models/tests/wgpu_nozi_graph_census.rs`, +1 more | **not a gap** -- norm+rope reach `rmsnorm.cu` and `rope.cu` through nv-layers; `attention_fp8_decode.cu` is the decode attention; the output gate (q3w_attn_gate) is candle sigmoid on CUDA in `qwen3_5_moe.rs` |
| `q3m_delta` | 14 | `nv-models/tests/graph_entry_census_oracle.rs`, `nv-models/tests/graph_q3d_delta_decode_oracle.rs`, `nv-models/tests/graph_q3d_delta_front_oracle.rs`, +4 more | **not a gap** -- the delta chain is `depthwise_conv1d_silu_bf16.cu` (conv), `gdn_gating.cu` (gating) and `gdn_decode.cu`/`gdn_recurrent.cu` (recurrent); the u4..u32/l32 entries are wgpu unroll specialisations of one CUDA kernel |
| `q3m_gemv_bf16` | 3 | `nv-models/tests/qwen3_5_moe_gemv_bf16_load.rs` | **not a gap** -- `gemv_bf16.cu` carries the bf16 gemv family, with the u4/u8 entries as wgpu unroll variants |
| `q3m_gemv_nvfp4` | 1 | `nv-kernels/tests/wgpu_gemv_nvfp4_variants.rs` | **not a gap** -- `gemv_nvfp4.cu`'s `nvfp4_gemv_bf16_kernel` is the same nvfp4 gemv |
| `q3m_gemv_nvfp4_v2` | 3 | none | **not a gap** -- the same nvfp4 gemv; fmlut/fdec/warp are three wgpu fp4-decode strategies, a search CUDA never needed because it decodes fp4 in hardware paths `gemv_nvfp4.cu` already uses |
| `q3m_moe` | 7 | `nv-models/tests/graph_entry_census_oracle.rs`, `nv-models/tests/graph_q3d_decode_head_oracle.rs`, `nv-models/tests/graph_q3d_elementwise_oracle.rs`, +2 more | **not a gap** -- `moe_route.cu`'s `moe_route_topk_kernel` (topk), `silu.cu`'s `silu_mul_kernel`, `moe_unpermute_scatter.cu` (combine) and `graph_decode.cu`'s `argmax_bf16_stage1_kernel`/`argmax_bf16_stage2_kernel`; gather_embed is candle gather on CUDA |
| `q3m_q8e` | 3 | none | **not a gap** -- `moe_gemv.cu` carries the per-expert gemv; `gemv_bf16.cu`'s `gemv_i8_normed_kernel` is the int8 form |
| `q3m_quant_lane_head_plain` | 0 | none | **fragment** -- no entry points; the plain head `qwen3_5_moe_wgpu.rs` prepends when instantiating the quant lane template |
| `q3m_quant_lane_head_silu` | 0 | none | **fragment** -- no entry points; the silu-fused head for the same template |
| `q3m_quant_lane_template` | 1 | none | **fragment** -- its entry is the QL_ENTRY_POINT placeholder -- `qwen3_5_moe_wgpu.rs` substitutes it at pipeline build |
| `q3m_quant_rows` | 2 | `nv-models/tests/qwen3_5_dense_wgpu_w8.rs`, `nv-models/tests/qwen3_5_moe_pass_budget.rs`, `nv-models/tests/qwen3_5_moe_quant_rate.rs` | **not a gap** -- `gemv_bf16.cu`'s `rowquant_i8_kernel` quantises the same rows; the silu-fused spelling lives in `quantize_nvfp4_bf16.cu` |
| `q3m_router_rank_template` | 1 | none | **fragment** -- its entry is the RR_ENTRY_POINT placeholder substituted by `qwen3_5_moe_wgpu.rs`; the routing math it instantiates pairs against `moe_route.cu` |
| `quant_gemv` | 14 | `nv-kernels/tests/fp8_contract_e4m3.rs`, `nv-kernels/tests/wgpu_fp8_group_gemv.rs`, `nv-kernels/tests/wgpu_gemv_nvfp4_scale_layout.rs`, +9 more | **wgpu-native by design** -- CUDA reaches these through cuBLASLt |
| `quantize_nvfp4_bf16_act_grid` | 2 | none | **not a gap** -- quantize_nvfp4_bf16.cu quantizes the same bf16 -> nvfp4 activations through nv_kernels_quantize_nvfp4_bf16; only the grid-stride spelling is wgpu's |

### CUDA host entry fns no test in `rust/crates/*/tests/` names

The `extern "C" int nv_kernels_*` fns are the CUDA API surface: one per callable path,
and what a test actually invokes. A file-level census reports the kernels below as
covered, because a sibling fn in the same `.cu` is tested. These are not.

| file | host fns no test names |
|---|---|
| `attention_fp8_decode.cu` | `nv_kernels_attention_fp8_decode_gscores`, `nv_kernels_attention_fp8_decode_paged` |
| `capture_copy.cu` | `nv_kernels_copy_cols_bf16`, `nv_kernels_mul_sigmoid_rowgate_f32` |
| `cutlass_probe.cu` | `nv_kernels_cutlass_flashinfer_probe` |
| `dflash_gate.cu` | `nv_kernels_softplus_gate_bf16` |
| `flash_decode.cu` | `nv_kernels_flash_decode_dev_f32_bf16out` |
| `gather_rows_bf16.cu` | `nv_kernels_gather_rows_bf16_strided` |
| `gemv_bf16.cu` | `nv_kernels_gemm_bf16_mk`, `nv_kernels_gemv_bf16_qkvg_normed`, `nv_kernels_gemv_e4m3_mk_h`, `nv_kernels_gemv_i8_mk_h`, `nv_kernels_gemv_i8_normed_mk_max_m`, `nv_kernels_gemv_i8_prenormed_mk`, `nv_kernels_gemv_q8_qkvg_normed`, `nv_kernels_normx_mk` |
| `graph_decode.cu` | `nv_kernels_rmsnorm_add_scale_bf16` |
| `hello.cu` | `nv_kernels_hello_launch` |
| `kv_ring.cu` | `nv_kernels_attention_bf16_decode_ring`, `nv_kernels_kv_ring_append_bf16`, `nv_kernels_kv_shift_bf16` |
| `laguna_step.cu` | `nv_kernels_laguna_qk_normrope_bf16`, `nv_kernels_laguna_rope_scale_bf16`, `nv_kernels_laguna_rstd256_bf16`, `nv_kernels_laguna_seqlens_prep`, `nv_kernels_prof_timestamp`, `nv_kernels_softplus_gate_exact_bf16` |
| `moe_route.cu` | `nv_kernels_gather_f32_by_ids`, `nv_kernels_moe_route_topk` |
| `moe_unpermute_scatter.cu` | `nv_kernels_moe_unpermute_scatter_tail` |
| `quantize_nvfp4_bf16.cu` | `nv_kernels_quantize_nvfp4_bf16_per_expert_strided`, `nv_kernels_silu_mul_quantize_nvfp4_bf16_per_expert_strided` |
| `sampler.cu` | `nv_kernels_softmax_topk_topp` |
| `silu.cu` | `nv_kernels_silu_mul_bf16_candle` |
| `tree_verify_attn.cu` | `nv_kernels_kv_append_bf16`, `nv_kernels_kv_compact_bf16` |
| `tree_verify_fp8.cu` | `nv_kernels_kv_compact_fp8` |

37 of 144 host fns; the ratchet in the script is 37. This is a *missing test* count, not a capability gap count -- both backends may well have the kernel.

Entry points with a recorded verdict, because a reader sent at them would otherwise guess:

| entry point | classification |
|---|---|
| `flash_decode.cu::flash_splitk_fused_fp8_derivev_kernel` | **capability gap** -- reconstructs V from the cached K on `attention_k_eq_v` layers; no wgsl entry does it, so the wgpu path reads a V it does not need to |
| `flash_decode.cu::flash_splitk_fused_fp8_mk_kernel` | **missing test** -- same as its bf16 sibling |
| `flash_decode.cu::flash_splitk_fused_mk_kernel` | **missing test** -- wgpu has six `_mk` entries in `flash_decode.wgsl` gated by `wgpu_flash_stage1_mk_unroll.rs` and `wgpu_flash_stage2_unroll.rs`; the CUDA side is the ungated one, so the fix is a CUDA-vs-CUDA gate and not a port |
| `laguna_step.cu::softplus_gate_exact_bf16_kernel` | **capability gap** -- one op spelled twice on CUDA, with `dflash_gate.cu::softplus_gate_bf16_kernel`, and neither on wgpu |

### Citations this file makes that must NOT resolve

Every other `.rs` / `.cu` / `.wgsl` / `.md` name and every backticked identifier in this
document must resolve in the tree, or `--check` fails. These are the recorded exceptions:
a name here that comes back to life fails the check too, because the sentence around it
would then be wrong in the other direction.

| name | why it is named here |
|---|---|
| `parity_gemm_nvfp4` | deleted by `9c74aedd2` (cull 157 -> 87 files); its numbers are quarantined in the historical table |

<!-- kernel-parity-census: END GENERATED -->
