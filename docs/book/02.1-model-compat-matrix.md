# Model compatibility matrix -- what this server can actually serve

Per-checkpoint blocker detail, complementing the model x backend matrix in
[`01.4-STATUS.md`](01.4-STATUS.md), which is the authority on which classes
serve on which backend. This page answers the different question: given a
checkpoint in the cache, what stops it, and is the blocker code or bytes.
Machine: an NVIDIA Blackwell workstation GPU (sm_120), 97887 MiB VRAM (95.6 GB).

## What "supported" means

Support is a property of the (family loader × on-disk naming convention × cached
bytes) triple, not of a format name. The dispatch surface is
`chat_engine/build.rs`'s `detect_family` -> `ModelFamily` -> `LoadedModel`
(02-model-loading.md); anything with no arm bails at `detect_family` with the
list of prefixes it accepts. Two standing traps:

- **Unknown `quant_method` falls through silently to `QuantScheme::None`.**
  `QuantizationConfig::from_hf_value` matches only the modelopt / NVFP4 / FP8 /
  compressed-tensors shapes and otherwise returns `Ok(Self::none())`, so a
  checkpoint declaring AWQ or GPTQ is treated as unquantized bf16 and fails
  later on a missing tensor, one layer from its cause.
- **`QuantScheme::{AwqInt4, GptqInt4, Marlin}` are declared and constructed
  nowhere** — a workspace grep finds only the enum definition. The live runtime
  set is `LinearKind::{Bf16, Fp8E4m3, Nvfp4, Mxfp4}`, and Marlin kernels reach
  the GPU through the `gemma4_e4b` w4a16 path, not through `QuantScheme::Marlin`.
  Delete the three dead variants or leave them until a checkpoint needs them.

Weights are safetensors on the CUDA path; GGUF loads through `GgufLoader` and
serves only on wgpu.

## Per-checkpoint blockers

| Checkpoint | Blocker |
|---|---|
| `RedHatAI/Qwen3.6-35B-A3B-NVFP4` | none |
| `nvidia/Gemma-4-31B-IT-NVFP4` | none |
| `google/gemma-4-31B-it` (bf16, 46G) | none; fits |
| `google/gemma-4-E4B-it` | serves on wgpu as the reference checkpoint; on CUDA it is behind `NV_E4B_CUDA_SERVE`, where selector and engine disagree on polarity (01.4-STATUS.md) |
| `google/gemma-4-26B-A4B-it` | **weights only.** `gemma4_moe.rs` and `gemma4_moe_wgpu.rs` are real and routing to `Gemma4Moe` is proven; the cached snapshot is GGUF plus 136-byte LFS stubs, so `WeightLoader::open_dir` dies at `no .safetensors files`. Fetching a safetensors snapshot is the whole fix. |
| `openai/gpt-oss-20b` | none on wgpu (its default); on CUDA behind `NV_GPTOSS_CUDA_SERVE`, which dequants mxfp4 to bf16 at load and so costs roughly 3x the native resident footprint |
| `Qwen/Qwen3.5-9B` | **weights only** -- config-only snapshot, needs the ~18 GB LFS fetch. The dense-hybrid decoder exists (`Qwen3Moe::from_loader_dense_quantized` on CUDA, `Qwen3_5DenseWgpu` on wgpu). |
| `Qwen/Qwen3.5-35B-A3B` | **weights only** -- same `Qwen3_5MoeForConditionalGeneration` family as 3.6-35B; bf16 ~70G fits |
| `Qwen/Qwen3.5-397B-A17B` | capacity, not support -- see below |
| `facebook/opt-125m` | no family, no impl, `.bin` only; test leftover |

**Qwen3.5-397B-A17B is a multi-GPU project, not a support gap.** Its
architecture is `Qwen3_5MoeForConditionalGeneration`, the already-supported
family, so nothing architectural blocks it; it is unservable here on three
independent grounds — only `config.json` is cached; 397B params is ~794 GB bf16
and ~211+ GB even at NVFP4's 4.25 bits/value, against 95.6 GB VRAM; and no
tensor parallelism exists to split it (`NcclComm` / `all_reduce` / `world_size`
grep to nothing). No code change on this machine makes it servable.

## Dense-hybrid layer mixing

Qwen3.5/3.6 checkpoints interleave `full_attention` and `linear_attention`
layers. `nv-layers/src/layer_mixer.rs` is the model-independent extraction and
the place to build against: `MixerKind` parses HF `layer_types` strings and
rejects anything but `full_attention` / `linear_attention`; `LayerMixPlan` is
built from explicit `layer_types`, from `full_attention_interval` (index `i` is
full iff `(i+1) % interval == 0`), or uniform, and carries
`full_slot_for_layer` / `linear_slot_for_layer` maps, counts, index lists and a
`matches_full_attention_interval` validator; `Mixed<F, L>` replaces a hand-rolled
per-model mixer generically and `LayerMixPlan::build(make_full, make_linear)`
constructs `Vec<Mixed<F, L>>` with per-layer constructor dispatch. Its six tests
are mutation-killed four ways: an interval off-by-one, a matcher that disagrees
with the builder, a layer handed both slots, and `build` passing the slot index
where it must pass the layer index.

`qwen3_5_moe.rs` still carries its own `LayerMixer` and is not migrated onto
`layer_mixer`; that migration is the remaining work, not a blocker.
`google/gemma-4-*`, `openai/gpt-oss-20b` and `google/embeddinggemma-300m` use
the same idiom with `sliding_attention` instead of `linear_attention`, so
`MixerKind` needs a third variant to cover them.

## Non-chat endpoint reachability

Serving reads models from the `rust/models/` nix-hug symlinks a
profile-carrying dev shell creates (`.#default` and `.#cuda` are
`profile = "full"` and do create them; `.#no-models`, `.#cuda-no-models` and
`.#local-hub` do not). **HF-cache presence is not reachability.**

| Checkpoint | Endpoint | Reachable | Notes |
|---|---|---|---|
| `Qwen/Qwen3-Embedding-0.6B` | `/v1/embeddings` | yes when `NV_EMBEDDING_MODEL_DIR` is set | model-level verified (unit-norm 1024-d); the route itself is tested only for the 503 path |
| `google/embeddinggemma-300m`, `nomic-ai/nomic-embed-text-v2-moe` | -- | no | `text_embeddings.rs` hardcodes `Qwen3Config`/`Qwen3`; a Gemma embedding config cannot parse, and NomicBert has zero references in the tree |
| `deepdml/faster-whisper-large-v3-turbo-ct2` | `/v1/audio/{transcriptions,translations}` | yes | ct2 backend |
| `Systran/faster-whisper-{tiny.en,base,large-v3,distil-large-v3}` | same | only by repointing the models dir | CT2-compatible, but `WhisperEngine::load` takes one pinned dir -- no per-request model routing |
| `pyannote/speaker-diarization-community-1` | `/v1/audio/diarization` | indirectly | never loaded directly; it is the export source for `scripts/export-diarizen-onnx.py`, which writes `rust/models/diarizen-segmentation.onnx` |
| wespeaker resnet ONNX | `/v1/audio/embeddings` | yes via `rust/models/wespeaker-resnet293-LM.onnx` | the filename says resnet**293** while the flake pin is resnet**34** |
| `speaches-ai/Kokoro-82M-v1.0-ONNX` | `/v1/audio/speech` with a kokoro model id | yes | -- |
| `Qwen/Qwen3-TTS-12Hz-0.6B-{Base,CustomVoice}` | `/v1/audio/speech` (`NV_TTS_TALKER_DIR`) | yes, real speech | synthesis round-trips the exact requested text through STT and an enrolled voice profile clones and round-trips. The **1.7B** checkpoints and `-VoiceDesign` were never run; synthesis is CPU-only on Metal; only wav/pcm are supported |
| `kyutai/pocket-tts-without-voice-cloning` | -- | no | zero consumers in the tree |
| `openai/privacy-filter` | `/v1/pii/*` | yes when `REDACT_MODEL_DIR` is set | loads `model.onnx` + `tokenizer.json` |
| `RedHatAI/gemma-4-31B-it-speculator.eagle3` | -- | yes as the Eagle3 drafter | not a servable model itself |

Internal, not model-serving: `silero_vad.onnx` and the smart-turn EOU model,
both from `rust/models/`, both wired.

## Ranked gaps -- models unblocked per fix

Count = cached checkpoints that go from unservable to genuinely servable here.

| Gap | Unblocks | Note |
|---|---|---|
| **Pure downloads, zero code**: safetensors snapshots for `gemma-4-26B-A4B-it`, `Qwen3.5-9B`, `Qwen3.5-35B-A3B` | 3 | every decoder already exists and routes |
| **MXFP4 native serving on CUDA** (`LinearKind::Mxfp4` through the quantized MoE path, instead of the load-time dequant to bf16) | 0 new, but reclaims ~2/3 of gpt-oss's CUDA residency, and MXFP4 is the AMD CDNA4-native format | the opt-in path exists; the native one does not |
| **Make unknown `quant_method` a hard error** | 0 | turns a one-layer-away missing-tensor error into an accurate one |
| **Embedding-model generalization** in `text_embeddings.rs` | 2 cached, low marginal value -- Qwen3-Embedding already serves the endpoint | |
| **Multi-model STT routing** (per-request whisper selection) | up to 4 cached CT2 variants, capability-redundant with the pinned large-v3-turbo | |
| **NCCL tensor parallelism** | 1 (Qwen3.5-397B-A17B) -- and still not on this machine: 211+ GB at 4-bit vs 95.6 GB VRAM | |

Not worth a slot: `facebook/opt-125m` (no impl, `.bin` only) and
`kyutai/pocket-tts` (zero consumers).

## diarized_json response shape

The shipped wire contract for
`POST /v1/audio/transcriptions?response_format=diarized_json`. Rust and Go
(`internal/stt/http.go`) must agree byte-shape; `client/test_diarized_json.py`
checks that.

```json
{
  "text": "...", "avg_logprob": 0.0, "no_speech_prob": 0.0,
  "segments": [
    {
      "type": "transcript.text.segment",
      "id": "seg_001",
      "speaker": "SPEAKER_00",
      "start": 5.2, "end": 12.8, "duration": 7.6,
      "text": "Hi, I need help with diarization.",
      "avg_logprob": -0.21, "no_speech_prob": 0.01, "confidence": 0.93
    }
  ]
}
```

- `type` and `id` are the OpenAI-required per-segment fields; `id` is `seg_NNN`,
  1-based, zero-padded to three. `start` / `end` / `duration` are seconds,
  converted from the engine's millisecond timings. `avg_logprob`,
  `no_speech_prob` and `confidence` are nullable extensions, `confidence`
  carrying the diarizer's per-turn confidence.
- `speaker` is `SPEAKER_NN` when diarization ran and `null` when it did not; in
  that case one segment is emitted per Whisper segment rather than per speaker
  turn. With diarization on, Whisper segments are bucketed into speaker turns by
  segment midpoint (nearest turn if it falls in none) and each turn's text is
  the joined text of its assigned segments.

**Why this shape.** OpenAI shipped `gpt-4o-transcribe-diarize` with
`response_format: "diarized_json"` and ElevenLabs' Scribe v2 markets an
"OpenAI-compatible format", so this is the converged industry shape. Extending
the transcription endpoint with a `response_format`, rather than inventing a
standalone diarization contract, buys compatibility with any client that already
speaks OpenAI's audio API.
