# Quality harness -- methodology, chat templates, and what framing costs

Canonical document for any claim of the form *"change X does / does not hurt
output quality"*, and for *what prompt each cached model actually expects*.

**Do not measure output quality with anything else.** If this harness is missing
a capability you need, extend it; a second harness is worse than either alone.

Companions: [`04.1-fp8.md`](04.1-fp8.md) (the quality evidence this harness produced),
[`08.4-PERFORMANCE.md`](08.4-PERFORMANCE.md) (speed, and the rule governing every
figure quoted here), [`05.1-wgpu-status.md`](05.1-wgpu-status.md).

---

## 0. The rules, in one place

1. **Always render through the model's own `chat_template.jinja`.** Never a
   hand-rolled wrapper, never ChatML, never a raw fragment.
2. **Always be EOS-aware.** Derive the stop set from `generation_config.json`,
   not `config.json`, and stop there.
3. **Always report per prompt, never pooled**, with at least one high-margin
   **control**.
4. **Never report forced-context agreement as if it were free-running.** They
   are different types in the harness for exactly this reason.
5. **Read the text.** A margin number is not a substitute; a degenerate loop is
   a *high*-confidence state.
6. **Check both feature sets before landing a cross-cutting edit.** A wgpu-only
   pass cannot see cuda-gated callers, and has reaped their API as dead three
   times.
7. **Never read a flip verdict without the instrument block printed under it.**
   ACCEPT/REJECT is one bit; the relative perplexity and the per-prompt KL table
   printed with it are what say whether a REJECT grazed the bar or blew through
   it by orders of magnitude, and whether the gate number came from all the
   prompts or from one. §5.3.

The deliverable of this document is not a number. It is that measuring the
wrong thing is now hard to do by accident.

---

## 1. Known hazards

Each is pinned by a test in `rust/tests/chat_eval.rs`.

### 1.1 Raw argmax agreement fails in both directions

It has hidden a real collapse behind a near-perfect *forced-context* number
whose free-running counterpart on the same run was under 10%, and it has
invented a fake collapse out of a raw un-templated prompt that read near-perfect
once chat-formatted and EOS-aware. The bf16 reference the collapse was scored
against was itself a repetition loop at a high median top-2 margin --
confidently wrong, so margin checks did not flag it.

An instruction-tuned model given the wrong framing sits on a knife edge between
ending its turn and continuing, so small numerical differences flip it and every
A/B reads as noise.

### 1.2 The default chat template is a silent ChatML fallback

`src/oapi/chat.rs` defines `ChatEngine::official_template()` with a default body
returning `None`; `render_chat()` then falls through to `render_chat_prompt()`,
which emits hardcoded ChatML
(`<|im_start|>user\n...<|im_end|>\n<|im_start|>assistant\n`). The silent-`None`
path is diagnosed (`log_missing_official_template`, plus an
`NV_REQUIRE_CHAT_TEMPLATE` env gate to hard-fail instead of falling back) and
`hazard1b_the_silent_none_path_stays_diagnosed` guards that -- but **the shape of
the hazard remains**: the trait default still returns `None`, so correct
templating is opt-in per engine. Against every cached model the fallback shares
no markers with the real generation prompt for every Gemma 4 variant and for
gpt-oss-20b. **The Qwen3.6 row is the dangerous one**: its official template
really *is* ChatML, so the fallback looks right -- it just omits the trailing
`<think>\n`, and a plausible-but-wrong prompt is worse than an obviously wrong
one.

Only one engine ever overrode the default and it was `cfg(feature = "cuda")`
throughout, so a `NVK_FEATURES=wgpu` build made the only override invisible.
`src/oapi/chat_engine_wgpu.rs` now carries a real wgpu override that also
*refuses to construct* without a template.

### 1.3 Two snapshots of the same model ship different templates

`models--nvidia--Gemma-4-31B-IT-NVFP4` has two cached snapshots -- `1365cf7a`
(12045 bytes, upstream Google's older template) and `e5ef03af` (16934 bytes,
`refs/main`, **authoritative**). They **share every weight blob** (symlink farms
into the same blob store, differing only in `tokenizer_config.json` and
`chat_template.jinja`) and for plain no-tools chat they render
**byte-identically**.

**So snapshot choice cannot affect any speed or plain-chat quality number.** It
absolutely can affect tool-calling: `1365cf7a` has no handling for
`role: "tool"` messages, emits a turn with the literal role `tool` (`<|turn>tool`
-- not a role Gemma 4 was trained on) and malformed tool declarations with a
stray leading comma. The extra bytes in `e5ef03af` buy native
`<|tool_response>...<tool_response|>` resolution, `reasoning`/`reasoning_content`
rendered as a thought channel, assistant-turn continuation, content-parts arrays
on system messages, `<|image|>`/`<|video|>` handling, nested-object tool schemas
and a changed `response_schema.x-regex`.

**Pin `e5ef03af`.** `ChatEvalModel::open()` refuses to guess when more than one
snapshot is cached and tells you to use `open_pinned`. Hardcoded `SNAPSHOT_DIR`
literals in `nv-models`/`nv-specdecode` tests still point at `1365cf7a`.

### 1.4 nvk.sh silently redirects the model hub

`nvk.sh` sources its cached devenv **after** caller env, and that devenv exports
`HF_HUB_CACHE=/nix/store/...-hf-hub-cache`. That hub carries only `e5ef03af` of
the NVFP4 model and **none** of gemma-4-31B-it, 26B-A4B-it, E4B-it, Qwen3.6 or
gpt-oss-20b. So under nvk.sh a lane that "auto-picks a snapshot" gets
`e5ef03af`, while a lane outside nvk.sh picks `1365cf7a` from
`~/.cache/huggingface/hub`. **That is the mechanism behind two lanes
disagreeing.** The harness searches every hub root, dedupes by snapshot id, and
prints which root each snapshot came from.

### 1.5 The stop set and BOS are wrong in `config.json`

Gemma 4's `generation_config.json` says `eos_token_id = [1, 106, 50]`
(`<eos>` / `<turn|>` / `<|tool_response>`); its `config.json` says `[1, 106]` --
**missing 50**. `StopSet::from_generation_config` reads `generation_config.json`
only. Qwen3.6 is `[248046, 248044]`; nothing about the Gemma numbers is portable.

`config.json` also has **no top-level** `bos_token_id` (it is nested under
`text_config`), so `parse_bos_id` reads a missing field and is saved by
`.unwrap_or(2)` -- right answer, by luck. And
`RedHatAI/Qwen3.6-35B-A3B-NVFP4` declares `bos_token: null` in
`tokenizer_config.json` but `bos_token_id: 248044` in `generation_config.json`,
and 248044 is also `pad_token_id` and an EOS id. **Any code that prepends "the
BOS from generation_config" to a Qwen prompt prepends an EOS token.** Gate BOS
on `tokenizer_config.json:bos_token` being non-null.

### 1.6 There is no "the Gemma4 format"

`there_is_no_such_thing_as_the_gemma4_format` renders four Gemma4 variants and
gets **two distinct** generation prompts: 31B-it, 26B-A4B-it and the NVFP4 build
append `<|channel>thought\n<channel|>` when `enable_thinking` is false; E4B-it
does not. **Hardcoding one family format is wrong for at least one variant.**

### 1.7 Greedy decode on the CUDA path is not bit-reproducible

Two runs of an *identical* configuration (same input ids, same model object,
same binary, greedy argmax) produced different termination behaviour on the
lowest-margin prompt while the other six prompts were byte-identical.
Corroborated independently: three back-to-back runs of a byte-identical CUDA
configuration produced **three different bf16 completions** while the fp8 arm
was bit-identical all three times ([`04.1-fp8.md`](04.1-fp8.md) §5.3), and the
**wgpu** bf16 arm was byte-identical across four invocations. **This is a
CUDA-path problem, not a property of the model.**

**Consequence: a single free-running trajectory on a low-margin prompt is not
reproducible evidence.** Use the high-margin controls for A/B and treat
low-margin open-ended prompts as descriptive only.

---

## 2. Chat-template inventory -- what each cached model expects

Reproduced by `rust/tests/chat_template_inventory.rs` (14 tests, CPU only,
~3 s). Snapshot lookup walks `NV_TEMPLATE_HUB`, then `HF_HUB_CACHE`, then
`$HOME/.cache/huggingface/hub`.

### 2.1 Which renderer each path uses

| Path | Renderer | Correct? |
|---|---|---|
| HTTP `/v1/chat/completions`, `NvEngineChat`, `--features cuda` | official `chat_template.jinja` via `ChatTemplate::render` | yes for plain chat, **no** for tools (§2.5) |
| HTTP, `NvEngineChat`, no `cuda` feature | ChatML fallback -- but `generate()` errors out anyway | n/a |
| HTTP, `WgpuChatEngine` | official template; **refuses to construct without one** | yes |
| HTTP, `EchoEngine` / any future engine | ChatML fallback, silently | no |
| `nv-models` / `nv-specdecode` test harnesses | hand-rolled string literals | mostly no (§2.6) |

### 2.2 Gemma 4 special tokens

All Gemma-4 variants share one tokenizer (`tokenizer.json` blob `cc8d3a0c...`),
one `generation_config.json` (`eos_token_id: [1, 106, 50]`, `bos_token_id: 2`)
and one special-token set:

| token | id | role |
|---|---|---|
| `<eos>` | 1 | EOS |
| `<bos>` | 2 | BOS |
| `<|tool_response>` | 50 | opens a tool response -- **also an EOS id** |
| `<|tool>` / `<tool\|>` | 46 / 47 | tool declaration |
| `<|tool_call>` / `<tool_call\|>` | 48 / 49 | tool call |
| `<|think\|>` | 98 | thinking-mode marker in the system turn |
| `<|channel>` / `<channel\|>` | 100 / 101 | channel open / close |
| `<|turn>` / `<turn\|>` | 105 / 106 | turn open / close -- **106 is end-of-turn** |

`<start_of_turn>` / `<end_of_turn>` (Gemma 2/3) are **not** special tokens in
Gemma 4 -- they tokenize as ordinary text.

Template bytes / bos / eos per snapshot: `gemma-4-31B-it` 12045, `26B-A4B-it`
18683, `E4B-it` 16317 and `Gemma-4-31B-IT-NVFP4` 12045 (`1365cf7a`) / 16934
(`e5ef03af`) all use `<bos>` / `<eos>`; `Qwen3.6-35B-A3B-NVFP4` is 7764 with
*null* bos and `<|im_end|>`; `gpt-oss-20b` is 16738 with `<|startoftext|>` /
`<|return|>`. None keeps a `chat_template` key inside `tokenizer_config.json`;
every one ships a standalone `.jinja`. `gpt-oss-20b` is inventory-only --
`detect_family` has no `gpt_oss` arm.

### 2.3 Rendered forms

Gemma 4 (31B-it, 26B-A4B-it, both NVFP4 snapshots). Assistant turns use role
`model`, not `assistant`; there is no newline between `<bos>` and the first
`<|turn>`. Tool declarations go in the system turn, in Gemma's own syntax (not
JSON), and a tool call's model turn is **never closed** -- the model resumes
inside the same turn, which is why `<|tool_response>` (id 50) is in the EOS set:

```
<bos><|turn>user\nWhat is 2+2?<turn|>\n<|turn>model\n<|channel>thought\n<channel|>
<|tool>declaration:get_weather{description:<|"|>Look up the weather<|"|>,parameters:{...}}<tool|>
<|turn>model\n<|tool_call>call:get_weather{city:<|"|>Oslo<|"|>}<tool_call|><|tool_response>response:get_weather{value:<|"|>{"temp_c": 3}<|"|>}<tool_response|>
```

`google/gemma-4-E4B-it` -- **no empty thought channel**; its prompts end at
`<|turn>model\n`, 28 bytes shorter. `RedHatAI/Qwen3.6-35B-A3B-NVFP4` -- no BOS;
the generation prompt **opens a `<think>` block** unless `enable_thinking` is
explicitly false, and `ChatTemplate::render` never passes `enable_thinking`, so
the served prompt is the thinking-open form. Its tool declarations are inlined
as JSON in a system turn and it is asked for
`<tool_call><function=...><parameter=...>` XML -- **not** the `<tool_call>{json}`
form the server's fallback preamble asks for. `openai/gpt-oss-20b` -- harmony
format, unrelated to both; a `system` message becomes a `developer` turn, and
`Current date` comes from `strftime_now`, which `chat_template.rs` stubs to
`1970-01-01`, so a served prompt would tell the model it is 1970.

`chatml_fallback_matches_no_served_model` asserts the ChatML fallback differs
from the official render of all seven snapshots.

### 2.4 The thought channel is grammar, not a leak

`<|channel>thought` is **part of the expected response grammar**;
`tokenizer_config.json.response_schema.x-regex` says so directly. The template
controls the mode: with `enable_thinking` unset or false (the served default)
the prompt ends `<|turn>model\n<|channel>thought\n<channel|>` -- an
**already-closed, empty** thought block, so the model must **not** produce
`<|channel>thought` itself and doing so is a genuine defect. With
`enable_thinking` true the system turn opens with `<|think|>` and the prompt
stops at `<|turn>model\n`, leaving the model free to open its own channel. E4B
is neither -- its prompt always stops at `<|turn>model\n`, so E4B opening its own
thought channel is **normal**. **Record which prompt form was used before
calling `<|channel>thought` a leak either way.**

### 2.5 Tools never reach the template

`render_chat()` calls `build_tool_messages()` when tools are present, then
`render_official(t, &msgs)` -- and `render_official` passes `tools: None` to
`ChatTemplate::render`. **The jinja `tools` variable is always undefined in
production.** Tools are flattened into a synthetic system message by
`tool_preamble()` asking for `<tool_call>{"name": ..., "arguments": ...}</tool_call>`,
and `role: tool` messages are rewritten to `user` text. For Gemma 4 the model
never sees `<|tool>declaration:...<tool|>`, is asked for a syntax it was not
trained on, and the `response_schema` parser cannot match what comes back.
`render_chat_drops_tools_before_reaching_the_official_template` pins this.

### 2.6 Prompt framing in the existing harnesses

| file | prompt it builds | verdict |
|---|---|---|
| `nv-models/tests/cuda_fp8_freerun.rs` | `<\|turn>user\n{p}<turn\|>\n<\|turn>model\n` + BOS id 2 at the call site | turn markers right, **missing** `<\|channel>thought\n<channel\|>`. **Retired** in favour of `PromptPack` |
| `nv-models/tests/gemma4_e4b_wgpu.rs` | token ids matching E4B's own template | **correct for E4B** |
| `nv-specdecode/tests/dflash_real_weights.rs` | `<start_of_turn>user\n...<end_of_turn>\n<start_of_turn>model\n` | **wrong model generation** -- Gemma 2/3 markers, tokenized as literal text |
| `src/oapi/chat_engine.rs` `render_gemma4_prompt` | `<bos>` + turn markers, ends `<\|turn>model\n` | missing the empty thought channel; reachable only when the jinja render errors |
| 23 Laguna harnesses | `〈\|EOS\|〉<user>{q}</user>\n<assistant></think>` | **22 ids where the shipped template renders 55** -- §4 |

The `cuda_fp8_freerun.rs` wrapper's guessed ids are a strict **prefix** of the
official ids and the entire residual is four tokens,
`[100, 45518, 107, 101]` = `["<|channel>", "thought", "\n", "<channel|>"]`.
**Four tokens is small and it would be easy to dismiss. Do not.** They are the
last thing the model reads before its first sampled token, and they decide
whether it opens a thought channel or answers directly. That is the knife edge.

---

## 3. Which framing is actually best

Each arm is scored **against the task**, independently, not by agreement with
another arm: different framings are *supposed* to produce different
continuations, so cross-arm argmax agreement would say nothing. Signals: clean
termination, control-token leakage *before* the end, answers-vs-continues,
objectively-checkable instruction following, and reading the text. Rates:
`perf/runs.jsonl`.

Arms: **A/B** official `chat_template.jinja` at the two NVFP4 snapshots (both
serving-valid, byte-identical input for plain chat), **C** the ChatML fallback,
**D** raw un-templated text, **E** the in-tree hand-written wrapper
(`chat_engine::render_gemma4_prompt`). All five render through the production
loader. Arm C is not merely "the wrong markers": Gemma 4's tokenizer has no
`<|im_start|>` token, so each marker shatters into seven literal sub-word
pieces.

What each arm costs, across `Gemma-4-31B-IT-NVFP4`, `google/gemma-4-E4B-it` and
`RedHatAI/Qwen3.6-35B-A3B-NVFP4` (greedy, per-model stop sets):

- **A/B (official).** The only serving-valid framing. Obeys every instruction
  including "nothing else" constraints, leaks no mid-sequence control tokens,
  ends its turn on every prompt, and sits at healthy control margins.
- **C (ChatML fallback).** On Gemma 31B it answers and then **invents the rest
  of the conversation** -- fake user and assistant turns, plus a corrupted
  marker because it is reproducing a string not in its vocabulary; where it
  terminates, that is the model rescuing the harness by opening a
  `<|channel>thought` block ChatML never asked for. That is why "ChatML looks
  like it works". On E4B it emits **`<|im_end|>` into user-visible output on
  every prompt** -- a termination-only metric passes it while a
  `/v1/chat/completions` user literally sees `<|im_end|>` on every reply, **the
  single most concrete quality cost of the fallback and invisible to any numeric
  metric**. On Qwen it is a different *mode*, not a catastrophe -- both it and
  arm A are valid Qwen grammar -- but it silently changes **who decides whether
  to think**, a serving-behaviour, latency and token-cost difference invisible
  to a text diff and **the strongest argument here for
  `NV_REQUIRE_CHAT_TEMPLATE=1`**.
- **D (raw).** The direct explanation of the historical false reading. Several
  prompts produce **no output whatsoever** -- first sampled token is `<turn|>` --
  at knife-edge margins an order of magnitude under the same questions' margins
  on the official template; the rest run to MAX-STEPS as degenerate loops at
  healthy-looking margins. **A degenerate loop is not a low-confidence state**,
  so a harness scoring "median top-2 margin" on a raw prompt sees a healthy
  number while emitting garbage. On E4B it echoes the question back and invents
  follow-up exam questions.
- **E (hand wrapper).** Behaviourally sound but textually contaminated: its
  prompt stops at `<|turn>model\n`, so the model correctly opens its own thought
  channel (documented grammar, not a leak) and every response begins with four
  tokens a consumer must strip.

**Termination rate is not a valid quality signal for a reasoning model at too
small a budget.** At 96 steps every Qwen3.6 arm looks broken and that reading is
wrong -- arm A pre-opens `<think>\n` and 96 tokens is not enough; at 320 steps
it resolves cleanly. E4B is far more robust to bad framing than 31B, so on E4B
the failures are qualitative rather than terminal.

---

## 4. What framing errors are worth, in acceptance points

**Eagle3, templated vs raw** (same binary, same session, minutes apart, only
`GEMMA4_PROMPT` differing; 31B NVFP4 verifier + the real `speculator.eagle3`,
k=4): the raw `"Hello, my name is"` fragment inflates acceptance well above the
templated `openended-explain` arm -- and that is the *benign* branch, where the
raw arm produced a coherent turn. The other branch of the same box
nondeterminism (§1.7) is a repetition loop, on which the in-repo guard fixture
shows near-total acceptance over a body that is one short token run repeated.
**So the framing error on Eagle3 acceptance spans from a mid-double-digit-percent
inflation to a multi-fold one, depending on which trajectory the box happens to
produce.**

**Laguna, framing x EOS-awareness** (`laguna_prompt_framing_ab.rs`,
Laguna-XS-2.1-NVFP4 + DFlash-NVFP4, k=4, max_new 96, stops `[2, 24]`). The
hand-built wrapper is **22 ids where the shipped template renders 55**, the
delta being the default Poolside system persona the literal drops. **The
historical Laguna configuration manufactured its own repetition loop**: the row
every published Laguna acceptance number came from is `hand-built + FREE-RUN`,
and nearly every generated token is past the stop token -- the model answered
`Paris` at token 1, the harness kept decoding, and it restarted its turn over
and over. **This is the control-token-leak signature this repo once read as
proof of kernel corruption, reproduced from a healthy model on a healthy kernel
purely by not stopping.** The EOS-aware arm of the identical configuration stops
at 2 tokens, and EOS-awareness alone, framing fixed, moves the accept rate
materially. **Thinking-mode is the larger framing error, not the persona**: the
persona alone moves free-run acceptance by a double-digit percentage, while
turning thinking on -- what `generation_config.json` ships as
`default_chat_template_kwargs.enable_thinking` -- moves it severalfold more and
turns a 1-token `Paris` into an unfinished 96-token reasoning trace. **The
acceptance damage is concentrated in short-answer prompts**; where all six arms
ran the full budget the spread is small. **The *text* damage is everywhere.**

**Two ways an acceptance number is void.** *Too short*: a control emitting a
two-token turn drafts 8 tokens and reports `accept 0.000` -- the "only CONTROL
rows are A/B evidence" rule defends against runaway trajectories, not too-short
ones, so controls remain excellent termination evidence and are not acceptance
evidence. *Too degenerate*: `accept 1.000` on a prompt that free-runs into a
restart loop, which is trivially predictable. Both push in **opposite**
directions, which is why an acceptance number without its trajectory health is
unusable.

**Harness hygiene.** `prompt_hygiene.rs` scans 54 real-weights harnesses (2
allow-listed, 23 disclosed-laguna, 0 new); 19 run a decode loop and 10 name no
stop set at all. **The Gemma 4 family is closed** (`eagle3_with_gemma4.rs` and
`lora_eagle3.rs` retired into templated + EOS-aware form); **Laguna retemplating
is 5 of 28 done, 23 disclosed -- not finished**, with
`laguna_attn_fp8_shape.rs`, `laguna_attn_w8_shape.rs`, `laguna_dflash_diag.rs`
and `verify_with_real_model.rs` still untemplated. The strengthened guard
surfaces one pre-existing defect:
`no_test_resolves_gemma2_markers_through_the_gemma4_tokenizer` fails on
`nv-models/tests/gemma4_e4b_wgpu.rs`, which builds a stop set from
`["<eos>", "<end_of_turn>"]` -- `<end_of_turn>` is a Gemma 2/3 spelling not in
the Gemma 4 vocab, so `token_to_id` returns `None`, the `filter_map` yields an
empty stop set, and the check passes because **it checks nothing**. Fix is
105/106 or `generation_config.json`.

---

## 5. How the harness makes the wrong thing hard

| Guarantee | Mechanism |
|---|---|
| **Un-templated use is impossible by accident** | `free_running()` and `forced_context()` take a `TemplatedPrompt`; the only public constructor yielding serving-valid provenance is `from_official_render`. The escape hatch is named `raw_untemplated_not_serving_evidence`, and both entry points reject it at runtime with a message naming the fix |
| **Templates are never hardcoded** | prompts render through `ChatTemplate::load(snapshot_dir)` -- the loader serving uses -- so a template bug is inherited rather than papered over |
| **Snapshots cannot be auto-picked** | `open()` errors when a repo has more than one cached snapshot. Every pack records the template digest and byte count |
| **A pack cannot be used against the wrong weights** | `PromptPack::load_for_snapshot(pack, weights_dir)` re-derives the digest from the weights directory it is about to load and refuses on mismatch. This is the only way prompts get into a GPU test file |
| **Forced-context cannot be reported as free-running** | distinct types; the forced accessor is literally named `upper_bound_agreement_is_not_serving_evidence()` |
| **Single-prompt and control-free claims are refused** | `SuiteReport::validate()` errors below 2 prompts or with no `PromptKind::Control` row. There is no pooled accessor at all |
| **A control that diverges is a hard error** | `assert_controls_exact()` |
| **EOS is derived, not assumed** | `StopSet::from_generation_config`; `free_running` stops there and records `StopReason`. `same_termination` is a first-class field |
| **Every divergence is scored, not just the first** | `divergence_margins` carries `(step, reference top-2 margin)` for every disagreeing step in the common prefix |

### 5.1 Reading a report

```
  prompt "openended-explain" [open-ended]  FREE-RUNNING agreement 8/24 (33.3%)
    first divergence Some(8), shape CASCADED, resynced_at None, post-divergence 0/15 (0.0%)
    top-2 margin at divergence 0.0200 vs reference median 6.0000 (ratio 0.003)
    16 disagreeing step(s) in the common prefix, 4 of them below 0.25x the reference median margin
    reads as: AMBIGUOUS POSITION that then cascaded: the step itself was a near-tie, so the
      downstream text difference is decoding amplification rather than proof of a bad kernel.
```

**Only the first divergence is independent evidence.** After the arms cascade
they decode different contexts, so the reference being decisive later is the
expected consequence, not a second fault. The count is useful in one direction
only: if *every* disagreement is a near-tie, the whole trajectory difference is
decoding amplification. **The margin ratio is the load-bearing column** -- 33.3%
agreement at ratio 0.003 is a coin flip the candidate lost; the same 33.3% at a
ratio near 1.0 is a real regression, and `divergence_reads_as()` states which.

### 5.2 Files, and the division of labour

| file | role |
|---|---|
| `rust/tests/chat_eval_core.rs` | dependency-light core: stop sets, prompts, free-running, comparison, suite report. `std` + `serde` + `anyhow` only, so any crate's tests can `#[path]`-include it |
| `rust/tests/chat_eval.rs` | templating over the **production** `oapi::chat_template::ChatTemplate`, plus the hazard guards and prompt-pack emission. No cargo features needed |
| `rust/tests/chat_template_inventory.rs` | §2's inventory, 14 CPU tests |
| `rust/tests/chat_template_arms_emit.rs` | renders all five arms of §3 through the production loader, writes a JSON arm-pack. CPU only |
| `rust/crates/nv-models/tests/chat_eval_arms.rs` | GPU arms; consumes a prompt pack; real-weight runs `#[ignore]` + env-gated |
| `rust/crates/nv-models/tests/chat_template_arms_cuda.rs` | CUDA arms for §3; loads the model once, free-runs every arm, prints the text |
| `rust/crates/nv-models/tests/fp8_contract_freerun.rs` | owns **forced-vs-free-running** and **teacher-forced quality** (rank + perplexity off the logits) -- a strictly stronger signal than argmax agreement |
| `rust/crates/nv-models/tests/fp8_contract_prompts.rs` | pack resolution + snapshot pinning shared by every GPU harness, the `RunTable` behaviour view, and `InstrumentReport` (§5.3) |
| `rust/crates/nv-models/tests/wgpu_default_flip_quality.rs` | the **default-flip** A/B batteries (31B, Qwen3.6, E4B) -- arms, anti-no-op guards, `FLIP-VERDICT` blocks, JSON artifact |

The core is deliberately in `rust/tests/` and included by path rather than
duplicated: `#[path = "../../../tests/chat_eval_core.rs"] mod evalcore;`.

**`fp8_contract_freerun.rs` is not a rival harness**; it owns a different axis
and owns it well. What it lacks is templating and stop handling. **They should
converge, two ways, and neither half is the obvious winner:** it should take its
prompts from a `PromptPack` and its stops from a `StopSet` instead of raw
strings and a fixed step count, and `chat_eval_core.rs` should grow a
perplexity/rank column ported from `teacher_forced_quality`. **Argmax agreement
is the weakest of the three signals and it is the one that failed in both
directions.**

### 5.3 Kernel flips vs weight-format flips -- what the bar does not tell you

The default-flip harness decides an arm on a bar whose load-bearing clause is
**G3c: worst-control mean `KL(reference ‖ candidate)` <= 1e-3 nats**, and that
bar is not negotiated per run. Every `FLIP-VERDICT` block prints the instrument
next to itself -- relative perplexity, plus a per-prompt table of mean KL / max
step KL / forced top-1 / rho, controls and open-ended separated, sorted
worst-first, and a count of prompts above a visible-signal floor. Reporting
only; no gate reads any of it.

**Why: 1e-3 was calibrated for KERNEL flips.** A kernel flip -- different
summation order, a fused pass, a rewritten GEMV -- computes the *same function*
two ways, so near-bit-identity is achievable and anything above float noise is a
bug worth blocking. A **weight-format flip** (bf16 -> int8, nvfp4 -> int8)
changes the function on purpose: re-quantization always shifts the distribution,
and the real question is whether the shift is small against what the model's own
decisions absorb. The bar answers only the kernel question either way, which is
why the perplexity and the table print *next to* it, not instead of it.

**What the bar does tell you**: that a candidate is *indistinguishable* from the
reference at the ~0.1%-perplexity level, roughly two decades below the 0.1-1%
gaps that separate shipped quantization recipes. **What it does not tell you**:
how far past it a failing arm is, whether the failing arm changes any observable
output, or whether the number is broad or came from one prompt. The worked
table (four weight-format flips under the same unchanged bar; values in
`perf/runs.jsonl`) has 31B nvfp4 -> int8 group-128 ACCEPTed token-exact far
under the bar, 31B nvfp4 -> int8 per-row REJECTed genuinely broken orders over
it, and E4B bf16 -> int8 REJECTed at *both* granularities while staying
token-exact with identical answer text -- two `REJECT`s orders of magnitude
apart in perplexity, which a one-word verdict hides entirely.

The independent lesson in that table: requantizing to a format **finer** than
the source is near-free *if the scale granularity can represent the source*
(nvfp4 is 4-bit with per-16 scales; int8 group-128 accepted, int8 per-row did
not), while quantizing from the model's **native** precision is genuinely lossy
and finer grouping does not rescue it.

**The max-over-prompts caveat, and why a verdict is pinned to its tree.**
"Worst-control mean KL" is a max over prompts of a mean over steps, and the
means are short -- the E4B controls terminate in 2-3 tokens, so 11 prompts
contribute 26 forced steps in total and only 4 of the 11 sit above the
visible-signal floor. The reported number *is* one prompt's number and it moves
when that prompt moves: the same E4B group-128 battery, same pack, same
controls, same forced steps, has returned REJECT and then ACCEPT hours apart
with the per-prompt table showing the entire difference was **one prompt out of
eleven**. That is not instrument noise -- the run was byte-reproducible, an A/A
repeat returning identical KL on every prompt. **Two runs read as a trend and
are not one; re-derive any archived verdict before quoting it.** Hence the
`SIGNAL CONCENTRATION` line: `n/N` prompts above the visible-signal floor (1e-6
nats ~ +0.0001% perplexity, override for reporting only with
`NV_FLIP_SIGNAL_FLOOR`), the worst-to-second-worst ratio, and a per-decade
histogram. Before comparing two runs, check that `n` and the identity of the
loud prompts match. **Adding prompts changes a max; it barely changes a median**
-- so the block prints pooled-over-steps and median-prompt perplexity next to
the worst-prompt one.

Machine-readable: the stdout line is
`FLIP-VERDICT <model> <flip> <verdict> | rel-ppl +/-x% worst-control-mean-KL ... signal n/N above 1e-6`,
and `flip-verdicts-<model>.json` carries an `instrument` object per arm with
every per-prompt row (controls and open-ended in separate arrays).

The §3 framing study deliberately has its own two files rather than bending
`chat_eval_*`, because `free_running()` **refuses at runtime** any prompt whose
provenance is not `OfficialTemplate` -- correctly, for its purpose -- while the
whole point of §3 is to run the *invalid* framings on the *same* model. The
right long-term shape is a `Provenance`-tagged "negative control" entry point in
`chat_eval_core.rs`, at which point that driver collapses into it.

---

## 6. Running it

CPU only, no cargo features, no GPU -- safe while another lane owns the GPU:

```sh
N="NVK_LANE=<lane> NVK_PKG=speaches-plus NVK_FEATURES="
$N rust/scripts/nvk.sh test --test chat_eval_core -- --nocapture
$N rust/scripts/nvk.sh test --test chat_eval -- --nocapture --test-threads=1
$N rust/scripts/nvk.sh test --test chat_template_inventory -- --nocapture   # NV_TEMPLATE_DUMP=1 dumps every form

# emit prompt packs (lands in $NV_CHAT_EVAL_OUT, default ~/.cache/nvk-tmp/chat-eval)
$N rust/scripts/nvk.sh test --test chat_eval emit_prompt_packs_for_the_measure_lane -- --nocapture
# emit the five-arm pack for the framing study
$N rust/scripts/nvk.sh test --test chat_template_arms_emit -- --nocapture --test-threads=1
```

Never write packs to `/tmp`.

Real weights -- the measurement lane owns GPU time; idle-gate first, run strictly
serially:

```sh
NVK_LANE=<lane> NVK_PKG=nv-models NVK_FEATURES=wgpu NV_CHAT_EVAL_TEST=1 NV_CHAT_EVAL_STEPS=96 \
  NV_CHAT_EVAL_PACK=~/.cache/nvk-tmp/chat-eval/pack-nvidia--Gemma-4-31B-IT-NVFP4-e5ef03af.json \
  NV_CHAT_EVAL_WEIGHTS=~/.cache/huggingface/hub/models--nvidia--Gemma-4-31B-IT-NVFP4/snapshots/e5ef03af... \
  rust/scripts/nvk.sh test --test chat_eval_arms -- --ignored --nocapture

NV_TMPL_ARMS_TEST=1 NV_TMPL_ARMS_STEPS=96 \
  NV_TMPL_ARMS_PACK=~/.cache/nvk-tmp/chat-template-arms/arms-gemma4-31b-nvfp4.json \
  NVK_LANE=<lane> NVK_PKG=nv-models rust/scripts/nvk.sh test --test chat_template_arms_cuda \
  chat_template_arms_free_running_on_real_gemma4_31b_nvfp4 -- --ignored --nocapture --test-threads=1
# swap pack + test name for ..._gemma4_e4b and ..._qwen36_35b_nvfp4;
# NV_TMPL_ARMS_ONLY=A,B restricts the arms; NV_TMPL_ARMS_STEPS=320 is the budget Qwen needs
```

`NV_CHAT_EVAL_HUB` overrides hub search order if nvk.sh's `HF_HUB_CACHE` is
pointing somewhere unhelpful (§1.4).

**Reading a skip correctly.** A `cfg`-out prints `0 passed` **and** `0 ignored`
-- that is a SKIP, not a pass. A genuine env-gated skip shows a **non-zero
ignored** count. `chat_eval_arms.rs` carries
`wgpu_arms_are_cfg_out_without_the_wgpu_feature`, which exists only when the
feature is off, so the two cases are distinguishable from the test list alone.
`chat_template_arms_cuda.rs` is `#![cfg(feature = "cuda")]` -- **never build it
with `NVK_FEATURES=wgpu` and believe the result.**

---

## 7. Coverage gaps

Not covered, and not to be assumed from what is: `openai/gpt-oss-20b` and
`google/gemma-4-26B-A4B-it`; the wgpu backend for the framing arms (every arm in
§3 is CUDA -- the framing conclusions are about the *prompt* and should carry,
the termination and nondeterminism results should not be assumed to); sampling
(greedy argmax only); tool-calling generation (§1.3 settles the template
difference statically); `enable_thinking=true` for Gemma 4 (covered for Laguna
only); and context beyond ~130 tokens total for the fp8 arms (see
[`04.1-fp8.md`](04.1-fp8.md) §9).

---

## 8. The needle-in-a-haystack retrieval gate (`rust/tests/niah_probe.rs`)

`niah_probe.rs` (`#[ignore]`, `NV_NIAH_TEST=1`, `cuda` feature) is the suite
that gates any future int4 or other lossy KV-cache tier, per model family,
before it ships. It plants a unique passphrase at K configurable depths inside
an N-thousand-token filler (`NV_NIAH_TRIALS`, `NV_NIAH_TOKENS`,
`NV_NIAH_DEPTHS`), sends each haystack through the real HTTP
`/v1/chat/completions` path against the production serving engine, and scores a
trial as passed only if the completion contains the exact planted passphrase.

**A KL or perplexity bar is not a retrieval bar.** §5.3's worked table shows a
weight-format flip can sit near the KL noise floor -- token-exact on every
control -- and still be judged on a statistic that averages a divergence over
every position in the sequence, while retrieval success depends on exactly one.
Error norms measured against a reference distribution provably do not predict
whether a compressed KV cache still finds the one token that matters at depth
0.9 of a multi-thousand-token context; only re-running the actual retrieval
does. Any int4 or other lossy KV-cache change must re-run this suite per model
family and match the bf16 baseline's x/K before it is eligible to ship -- a
passing distributional gate elsewhere in this document is not a substitute.

---

## Appendix A. Published quality and throughput baselines

Comparison targets for the cached checkpoints, web-sourced rather than measured
here. Derived arithmetic is labelled `derived`; secondary (non-primary) sources
are marked `(secondary)`.

### A.1 Quality rankings

| Checkpoint | MMLU-Pro | GPQA Diamond | AIME | LiveCodeBench v6 | Arena / index |
|---|---|---|---|---|---|
| `google/gemma-4-31B-it` (bf16 ref for NVFP4) | 85.2 | 84.3 | 89.2 (2026, no tools) | 80.0 | LMArena Elo ~1452, #3 open (secondary); AA Intelligence Index 29 |
| `Qwen/Qwen3.6-35B-A3B` (bf16 ref for NVFP4) | 85.2 | 86.0 | 92.7 (AIME26) | 80.4 | AA Intelligence Index 32 |
| `Qwen/Qwen3.5-35B-A3B` | 85.3 | 85.35 | -- | -- | -- |
| `google/gemma-4-E4B-it` | 69.4 | 58.6 | 42.5 (2026) | 52.0 | -- |
| `Qwen/Qwen3-Omni-30B-A3B-Instruct` | -- | claims > Qwen3-235B-A22B non-thinking on GPQA/AIME25/ZebraLogic; SOTA 22/36 audio-video benchmarks | -- | -- | AA Index 11 (non-reasoning) |
| `deepseek-ai/DeepSeek-OCR-2` | n/a | OmniDocBench v1.5 overall 91.09%; parse edit-dist 0.100 @ ~1120 vis tokens vs Gemini-3 Pro 0.115 | -- | -- | -- |
| `Qwen/Qwen3-Embedding-0.6B` | n/a | MTEB Multilingual mean 64.33; MTEB English v2 70.70; C-MTEB 66.33 | -- | -- | -- |
| `RedHatAI/gemma-4-31B-it-speculator.eagle3` | n/a (draft head) | accepted length 2.07-3.15 @ k=3, 2.20-3.93 @ k=5; claimed 2.0-2.5x single-stream speedup (vLLM, preliminary) | -- | -- | -- |

Sources: the [Gemma 4 model card](https://ai.google.dev/gemma/docs/core/model_card_4),
[Qwen3.6-35B-A3B](https://huggingface.co/Qwen/Qwen3.6-35B-A3B) and
[RedHatAI NVFP4](https://huggingface.co/RedHatAI/Qwen3.6-35B-A3B-NVFP4) cards,
[Artificial Analysis](https://artificialanalysis.ai/), the
[Qwen3-Omni](https://arxiv.org/pdf/2509.17765) and
[DeepSeek-OCR 2](https://arxiv.org/pdf/2601.20552) papers, and the
[Qwen3-Embedding](https://huggingface.co/Qwen/Qwen3-Embedding-0.6B) and
[speculator](https://huggingface.co/RedHatAI/gemma-4-31B-it-speculator.eagle3) cards.

The Gemma 4 card publishes no plain MMLU, IFEval, HumanEval, MBPP or LiveBench.
Qwen3.6-35B-A3B is 35B total / 3B active, 256 experts (8 routed + 1 shared), 40
layers alternating Gated DeltaNet / Gated Attention, 262k native context (agentic
SWE-bench Verified 73.4, Terminal-Bench 2.0 51.5). Gemma 4 E4B is 8B total /
4.5B effective (per-layer embeddings), 42 layers, 128k context.

**Headline:** the two 31-35B checkpoints are near-tied on MMLU-Pro (85.2 both);
Qwen3.6 leads GPQA (86.0 vs 84.3), AIME (92.7 vs 89.2) and AA index (32 vs 29).
E4B is a full quality tier below.

### A.2 Quantization quality cost (same base model, bf16 -> quant)

| Base model | Scheme | Benchmark | bf16 | quant | Recovery |
|---|---|---|---|---|---|
| gemma-4-31B-it | NVFP4 (ModelOpt v0.42.0, e2m1 + e4m3 scales, group 16, W+A) | GPQA Diamond | 85.80 | 85.35 | 99.5% |
| | | AIME 2025 | 87.92 | 87.60 | 99.6% |
| | | MMLU-Pro | 85.25 | 84.94 | 99.6% |
| | | LiveCodeBench pass@1 | 82.49 | 82.27 | 99.7% |
| | | SciCode subtask pass@1 | 33.61 | 33.18 | 98.7% |
| Qwen3.6-35B-A3B | NVFP4 (llm-compressor, W+A; lm_head, vision, MoE gates, embeds, linear-attn excluded) | GSM8K Platinum | 95.73 | 96.08 | 100.4% |
| | | IFEval | 93.09 | 92.45 | 99.3% |
| | | AIME 2025 | 92.92 | 91.25 | 98.2% |
| | | GPQA Diamond | 84.51 | 84.68 | 100.2% |
| | | LiveCodeBench v6 | 77.33 | 74.67 | 96.6% |
| | | MMLU-Pro (chat) | 85.32 | 84.70 | 99.3% |
| gemma-4-E4B-it | QAT w4a16 int4 group-32 compressed-tensors | -- | -- | -- | qualitative only ("preserving similar quality to bfloat16"); **no numeric table published** |
| (format-level) | MXFP4 (group 32, e8m0) vs NVFP4 | pretrain loss rel. error | -- | -- | MXFP4 ~2.5% vs NVFP4 ~1.5%; NVFP4 better via group 16 + fractional e4m3 scales |

**These suites are sensitive enough to detect real degradation** -- they do
register drops (SciCode 98.7%; LiveCodeBench 96.6%, -2.66 pts absolute). So the
~99% headline is not an artifact of saturated benchmarks: **coding pass@1 is
where 4-bit visibly costs**, knowledge/math benchmarks are within noise. Both
recovery tables are vendor-published (NVIDIA, Red Hat); no independent
replication exists for these exact checkpoints.

### A.3 Apple Silicon throughput baselines

The comparator set is the llama.cpp/Metal Apple-silicon perf thread rows
(LLaMA-2 7B across F16/Q8_0/Q4_0 on Ultra-class Apple silicon) plus secondary MLX/Ollama rows
for MoE and E4B-class checkpoints, against this repo's two local rows
(gemma-4-E4B-it bf16 on the Ultra-class box over HTTP e2e, and on the mid-tier box in the test
harness). All rates: `perf/runs.jsonl` -- the local rows' original logs are not
in the tree, the local prompt was one of four in circulation (a prompt-length
change alone moves e2e ms/tok by a percent-class amount on E4B's chunked
prefill), and both were superseded within a day, which is why this section is a
pointer rather than a table. Source for the llama.cpp rows: the
[Apple-silicon perf thread](https://github.com/ggerganov/llama.cpp/discussions/4167).
No published bf16 result for gemma-4-E4B-it on Ultra-class Apple silicon exists; the LLaMA-2 7B
F16 row (13.5 GB weights vs our 15 GB) is the closest comparator.

**Bandwidth bound (derived).** The Ultra-class peak is 800 GB/s. Reading all 15 GB of
bf16 weights per token caps at `800 / 15 = 53.3 tok/s`. llama.cpp's 7B F16 row
sustains a large fraction of peak; applying that efficiency to 15 GB gives the
realistic mature-stack target, and the local measurement sits severalfold below
it. The gap is likely wider still: E4B is 8B-total / 4.5B-effective, so a stack
streaming only the ~9 GB of active weights has a much higher ceiling. Dispatch
overhead is the leading candidate -- the per-pass average at 762 GPU
passes/token is command-encoder-latency-class, not bandwidth. **The local Ultra-class bf16 rate is not competitive.**

**Never derive effective bandwidth from the whole-checkpoint size.** It includes
the vision/audio towers and the 1.25 GiB embedding + 5.25 GiB
per-layer-embedding (PLE) tables, none of which the decode path reads in full
per token. Two corrected numerators exist and they disagree, about the PLE
tables specifically: `weight_bytes_per_token()` counts **one PLE row** per token
(like an embed lookup) at 9.288 GB/token, while safetensors-header bucketing
streams the **full 5.803 GB PLE/altup/laurel block** every token at
14.311 GB/token. Whether the E4B decode path reads per-layer embeddings as a row
gather or as a dense matrix is an empirical question that is **not settled**, so
both stand rather than one being picked. Either way the paragraph above is
unchanged in direction and understated in size: both corrected numerators put
effective bandwidth below any whole-checkpoint figure, so the gap to a mature
stack is **wider**, not narrower. The row-gather numerator is derived in
`docs/bytes-vs-decode-deconfound.md`; the bucketing numerator is re-runnable but
its script and log are not in the tree.

### A.4 Recommendation (192 GB Ultra-class Apple silicon)

`RedHatAI/Qwen3.6-35B-A3B-NVFP4` is the best quality on hand, narrowly over
`nvidia/Gemma-4-31B-IT-NVFP4` (which holds the arena Elo edge and is the pick if
arena-style chat preference matters more than reasoning benchmarks); NVFP4 costs
<=1 pt on everything except coding pass@1 (A.2). It is also the best quality per
token of latency, and not close -- ~3B active params at 4-bit is ~2 GB
read/token against ~16 GB for a dense 31B at 4-bit, an ~8x bandwidth advantage
at near-identical benchmark quality, which published Apple-silicon MoE results
bear out (`mlx-community/Qwen3.5-35B-A3B-4bit` where MLX is the runnable stack).
`gemma-4-E4B-it` is a tier below on quality and, served bf16, slower than
published 4-bit MoE numbers for far stronger models; its niche is
smallest-footprint serving only. `Qwen3-Omni-30B-A3B` is for audio/omni I/O
only. The Eagle3 speculator is a vLLM/GPU artifact with no Apple-silicon or wgpu
integration data.

No published data exists for: bf16 tok/s for `gemma-4-E4B-it` on Ultra-class Apple silicon under
any mature stack; a numeric bf16-vs-QAT recovery table for
`gemma-4-E4B-it-qat-w4a16-ct`; independent (non-vendor) replication of the
NVIDIA and Red Hat NVFP4 recovery tables; Apple-silicon throughput for
`Qwen3-Omni-30B-A3B` and `DeepSeek-OCR-2`; or NVFP4-on-Metal feasibility -- all
NVFP4 deployment data is vLLM/Blackwell, and the MLX/GGUF conversions are
requantizations, not NVFP4 execution.
