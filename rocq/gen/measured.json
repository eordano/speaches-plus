{
  "_comment": "Pointers to the CURRENT measurement artifacts. gen.py parses these at generation time so every re-bench re-derives the efficiency ratios instead of trusting a stale analysis. Artifacts live IN-REPO under docs/measurements/ and are regenerated by rocq/gen/bench.sh.",
  "server_log": "../../docs/measurements/2026-08-10-rocq-repoint/server-prof.txt",
  "bench_json": "../../docs/measurements/2026-08-10-rocq-repoint/bench.json",
  "min_rounds": 50,
  "min_rounds_comment": "GRAPHED SUMMARY lines with fewer rounds are warmup (the first summary has rounds=32 and a 91 ms draft outlier from lazy init); they are excluded from the lo/hi envelope",
  "k_chain": 4,
  "k_chain_provenance": "promoted default NV_EAGLE3_K=4 (main @ 45facc9); asserted against the log's 'cond_mode=shift k=4' lines at generation time"
}